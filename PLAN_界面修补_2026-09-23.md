# PLAN · 界面修补 7 项（2026-09-23）

> 状态：**待确认**，未动任何源码。示意稿：`_preview/plan_ui_patch_2026-09-23.html`
> 检测脚本（只读，可复跑）：`_probe/audit_destiny_combat.js`、`_probe/audit_destiny_tech.js`

---

## 一、检测结论

### ① 命格 · 战斗相关效果
46 个命格（白10/绿10/蓝10/紫7/金9；attr 24 + combat 22），战斗字段 20 种，**19 种有读取通道**：

| 通道 | 字段 | 状态 |
|---|---|---|
| `getDestinyBonus`（无 type 限制） | critRate / dodgeRate / counterRate / firstStrike / lifesteal / recoverPct / thorns / controlImmune / extraAttack / executeBonus / swordCritRate / noElemSpellMul / tribBonus | 全部生效 |
| `getDestinyAttrBonus`（无 type 限制） | 六维 wu/ti/dun/shen/dao/ling | 全部生效 |
| `applyDestinyYearly` | stonePerYear / wuPerYear(+Cap) / tiPerYear(+Cap) | 全部生效 |
| `getDestinyAttrMult`（**要求 type==='combat'**） | atkMul 6 条（全 combat）✅；defMul 7 条（**6 combat + 1 attr**）❌ | 1 条死配置 |

**唯一问题**：`houtu` 厚土之体（绿阶 · `type:'attr'`）带 `effect:{defMul:0.05}`，但 `engine.js:914` 的 `getDestinyAttrMult` 限定 `dest.type==='combat'` → 「防御 +5%」会显示给玩家却永不生效。
与 2026-09-18 修掉的 tribBonus（天命之子/天道宠儿，同为 attr）同类。
**修法**：去掉 `engine.js:914` 的 type 限制（推荐）／把该命格 type 改 combat／把 defMul 换成 def 数值。

### ② 功法（102 门）实装
效果**全部实装、无死效果**：
- 心法 12 个附加效果全部有 getter 且被调用：`mult / hpMax / hpMul / mpMul / atkMul / defMul / critPct / atkSpd / spellMul / craftTimeReduce / guard / thorns`
- 法术全部走唯一入口 `applySpellEffects()`：`dmg/cost/buff(+*Dur)/debuff/stun/dotBurn/dotPoison/disaster/heal/mpRestore/lifesteal/element`
- 遁术 `flee / guard` 生效

**文案缺失 3 处**：

1. **师父传功·择一亲传面板**（`ui.js:7851 showMasterPick`）——只有 名称+品质+一句 desc，无类型、无数值。
2. **图鉴·功法 tab**（`ui.js:8144`）——`eff: t.mult ? '修炼 ×N' : ''`，法术/遁术的 `eff` 恒为空串（102 门里只有 37 个心法有内容）。
3. **功法页「可装备法术」行**（`ui.js:5707` 与 `7143` 两处重复）——`x.slow ? '缚敌之霜' : …` 是死分支，全仓库无任何功法带 `slow`。

> 口径建议：desc 只放风味文案，数值统一由「选择页/功法页/图鉴」的 stat 行渲染（护山心经 desc 漏了 `guard 0.10` 与 `hpMax +50`；石壁遁/影遁术 desc 不写逃脱%与减伤%）。

---

## 二、五项改动

| # | 诉求 | 落点 | 改法要点 |
|---|---|---|---|
| ① | 修为满时按钮不显示 98% | `ui.js:243-254` | `canBreak` → `'突破（修为已满）'`；大境界仍为 `'渡劫 · 金丹劫'`；概率只在突破弹窗内展示（已有）。保底提示保留：`突破（修为已满） · 下次必成`。 |
| ② | 功法页【遗忘】+ 品质降序 | `ui.js:5599`（心法5651/法术5705/遁术5752）与 `ui.js:7075`（7087/7141/7188），共 6 处循环 | 在 `切换/装备` **左侧**插 `遗忘`；已装备项不给遗忘按钮；二次确认走全局 `uiConfirm(..., {okDanger:true})`；三段列表末尾统一 `.sort(byGradeDesc)`（`GRADE_ORDER=['黄','玄','地','天','仙']`）。新增 `Engine.forgetTech(s,id)`：先自动卸下 → 从 `s.techs` 移除 → `refreshStats` → `saveState`。 |
| ③ | 装备加成展示全量 | `ui.js:3814-3820` 与 `ui.js:6846-6852` | `equipStats()` 返回 10 字段、UI 只显示 5 个。补：`def / critPct / atkSpd / recover / mpPct / hpPct`。建议抽 `equipSumText(es)` 复用两处。 |
| ④ | 师父传功用「雾中奇遇」同款选择页 | 重写 `ui.js:7850-7873` | 改为 `showChapter + choices`（`.choice-btn`），选项含 `[类型] + desc + 数值`；抽共用 `techChoiceText(id)`，残魂传承（`ui.js:1726`）也改调它；结算仍走 `Engine.sectMasterResolve`（不变）；补「婉言谢绝」出口。 |
| ⑤ | 静室增高 + 撤离字号对齐 | `ui.js:1537` + `css/style.css:1380` | 根因：静室按钮复用了 HUD 小按钮的 `.adv-retreat`（`font-size:12px`）→ 改类名为 `rest-retreat`，继承 `.btn-main` 的 16px。增高：加 `#modal.modal-rest .modal-inner{min-height:46dvh;max-height:92dvh}`。`index.html:560` 的 HUD 小按钮不动。 |

---

## 三、待拍板

1. **厚土之体 defMul 死配置修不修？**（改引擎 → 需跑全量 358 用例）
2. **遗忘是否限制次数 / 返还资源？**（默认无限制、无返还）
3. **静室「加高弹窗」还是「改独立整页」？**（默认加高；改整页会牵动 `advAdvanceToMap()` 返回路径）

落地顺序建议：③ → ⑤ → ① → ② → ④ → 决策 1 的引擎修正。
