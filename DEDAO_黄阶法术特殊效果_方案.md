# DEDAO 黄阶法术特殊效果布局 · 方案（PLAN · 待执行）

> 状态：**已定稿待执行**（2026-09-14 PLAN 轮）。执行前的三项决策已由用户拍板，见第 2 节。
> 上游文档：`DEDAO_法术效果全等级大表.md`（档位表）、`DEDAO_法术新机制方案.md`（五机制×三阶）。
> 本轮不新增法术 id，不改数值以外的手感，只做「把五大机制补进黄阶」。

---

## 1. 背景与现状

五大特殊机制（眩晕 / 冻结 / 灼烧 / 中毒 / 伐灾）目前只覆盖 **玄 / 地 / 天** 三阶，
黄阶法术只有「元素效果」（暴击 / 攻击 / 回灵 / 减伤 / 吸血），机制列在档位表里全是「—」。

| 五行 | 元素效果（黄阶已有） | 五大机制（现状） |
|---|---|---|
| 金 | `buff.critUp` +8% / 2 回合 | 伐灾 `disaster` —— 黄阶无，玄起 |
| 火 | `buff.atkUp` +12% / 3 回合 | 灼烧 `dotBurn` —— 黄阶无 |
| 水 | `mpRestore` 10% | 冻结 `stun` —— 黄阶无 |
| 土 | `buff.defUp` −12% / 2 回合 | 眩晕 `stun` —— 黄阶无 |
| 木 | `lifesteal` 30% | 中毒 `dotPoison` —— 黄阶无 |

**引擎侧几乎无需改造**：`applySpellFx` / `bossCastSpell` / `tickDot` 全部按 `sp.grade` 通用取档
（`dotCapByGrade(sp.grade)`），`stun` 走概率、DoT 走叠层，都是数据驱动。
仅 `dotCapByGrade` 的黄阶分支目前靠 `else` 兜底返回 1，建议显式化。

---

## 2. 已确认的三项决策

| # | 决策点 | 结论 |
|---|---|---|
| 1 | 实现形态 | **挂到现有 9 个黄阶法术**（不新增法术 id；剑气诀无属性不加机制） |
| 2 | 生效范围 | **敌我双向生效**（与「所有 BUFF/DEBUFF 双向」既定口径一致） |
| 3 | 伐灾免控门槛 | **顺带修：门槛按阶取小** = `min(3, 本档上限)`，地/天 行为不变 |

---

## 3. 黄阶档位数值（四阶对照）

| 机制 | 字段 | 黄（本轮新增） | 玄 | 地 | 天 |
|---|---|---|---|---|---|
| 眩晕（土） | `stun` | **10%** | 20% | 40% | 80% |
| 冻结（水） | `stun` | **10%** | 20% | 40% | 80% |
| 灼烧（火） | `dotBurn` | **+1 层 / 次，上限 1** | +1，上限 2 | +2，上限 4 | +3，上限 8 |
| 中毒（木） | `dotPoison` | **+1 层 / 次，上限 1** | +1，上限 2 | +2，上限 4 | +3，上限 8 |
| 伐灾（金） | `disaster` | **+1 层 / 次，上限 1** | +1，上限 2 | +2，上限 4 | +3，上限 8 |

- 取「最低档」的理由：黄阶法术**同时保留原有元素 buff**，若机制数值再给高，会反超玄阶纯机制法术。
- 灼烧 / 中毒黄阶上限 1 → 单次施法烧 1 回合（10% 当前血）即消退；想叠层仍需上玄阶。
- 灵根亲和依旧**只放大 `dmg`**，不放大上述任何数值（沿用既有口径）。

---

## 4. 逐法术改动清单（`js/data.js`，8 行）

| id | 名称 | 五行 | 现有附加效果 | 新增字段 | 说明 |
|---|---|---|---|---|---|
| `jinren` | 金刃术 | 金 | critUp +8%/2 | `disaster: 1` | 金语义 = 暴击 + 伐灾 |
| `leiyin` | 雷音引 | 金 | critUp +8%/2 | `disaster: 1` | 与火/水「双法术同效果」对称 |
| `jianqi` | 剑气诀 | 无·青云剑宗 | critUp +8%/2 | **不加** | 无属性不参与五行机制（同万剑归宗 / 破天一击） |
| `huoqiu` | 火球术 | 火 | atkUp +12%/3 | `dotBurn: 1` | |
| `yuhuo` | 御火诀 | 火 | atkUp +12%/3 | `dotBurn: 1` | ⚠ 商店可购，早期即可得灼烧 |
| `shuidan` | 水弹术 | 水 | mpRestore 10% | `stun: 0.10` | 文案自动为「冻结」（按 element 判文案） |
| `hanshuang` | 凝霜诀 | 水 | mpRestore 10% | `stun: 0.10` | ⚠ 商店可购 |
| `luoshi` | 落石术 | 土 | defUp +12%/2 | `stun: 0.10` | 文案为「眩晕」 |
| `tengman` | 藤蔓术 | 木 | lifesteal 30% | `dotPoison: 1` | 木语义 = 吸血 + 中毒 |

同时更新这 8 条的 `desc`，把机制写进描述（沿用玄/地/天 机制法术的写法，如「（10% 眩晕）」）。

> 施法文案已按 `element` 自动分流（`sp.element === '水'` → 冻结，否则眩晕），
> 灼烧 / 中毒 / 伐灾 的 `out.push` 文案也已就绪，**无需新增文案分支**。

---

## 5. 引擎改动清单（`js/engine.js`，2 处）

### 5.1 `dotCapByGrade` 显式补黄阶

```js
// 现：黄阶靠 else 兜底返回 1
function dotCapByGrade(g) { return g === '天' ? 8 : (g === '地' ? 4 : (g === '玄' ? 2 : 1)); }
// 改（纯显式化，行为不变，便于测试断言）
function dotCapByGrade(g) { return g === '天' ? 8 : (g === '地' ? 4 : (g === '玄' ? 2 : (g === '黄' ? 1 : 1))); }
```

### 5.2 伐灾免控门槛「按阶取小」

现状：`DISASTER_IMMUNE_COST = 3` 固定，而黄(上限1)/玄(上限2) 叠不到 3 → **低阶伐灾永远摸不到免控**（只有地4/天8 能触发）。

改法：新增战斗态字段 `b.disasterCap`，在施放伐灾时记录**本次档位上限**（多次施法取 max）：

```js
// applySpellFx 伐灾分支内
b.disasterCap = Math.max(b.disasterCap || 0, dotCapByGrade(sp.grade));
b.disasterStacks = Math.min(dotCapByGrade(sp.grade), b.disasterStacks + sp.disaster);
```

免控判定改为按记录的上限取小：

```js
// applyPlayerControl / 免控校验
const cost = Math.min(DISASTER_IMMUNE_COST, b.disasterCap || DISASTER_IMMUNE_COST);
if (b.disasterStacks >= cost) { b.disasterStacks -= cost; /* 免控 */ }
```

| 施法档位 | 本档上限 | 免控消耗 | 与旧版对比 |
|---|---|---|---|
| 黄 | 1 | **1** | 从「不可达」变为可达 |
| 玄 | 2 | **2** | 从「不可达」变为可达 |
| 地 | 4 | 3 | 不变 |
| 天 | 8 | 3 | 不变 |
| 旧存档 / 手工置层（无 `disasterCap`） | — | 3（回落旧阈值） | 不变 |

- `ensureBattleFx` 补 `b.disasterCap = 0`（旧存档回落 → 阈值 3，保持兼容）。
- 该写法**保住既有回归**：`11-dead-config` 的「新机制⑤」手工置 `disasterStacks = 3`（无 cap）→ 阈值仍为 3，断言不变。

### 5.3 明确不改的部分

- `stun` / `dotBurn` / `dotPoison` / `tickDot` / `bossCastSpell` 的既有通用分支**一行不动**（数据驱动已足够）。
- `bossCastSpell` 不读 `sp.disaster` → **金系伐灾天然只惠及玩家**（与 `BOSS_ELEMENT` 中「伐灾为玩家专属」的注释一致）。
- UI 层无改动：法术描述来自 `desc`，战斗日志来自引擎 `out.push`，均自动生效。

---

## 6. 敌人影响面（决策 2「双向生效」的代价）

**14 个敌人**在用黄阶法术，届时会同步获得机制：

| 表 | 敌人 | 施法率 | 涉及黄阶法术 | 新获得机制 |
|---|---|---|---|---|
| `BOSS_ELEMENT` | 匪首 | 15% | 金刃术 / 雷音引 / 落石术 | 伐灾(自身) / 10% 眩晕 |
| | 黑山老妖 | 25% | 藤蔓术 | 中毒 1 层 |
| | 试炼之主 | 15% | 金刃术 / 火球术 / 落石术 / 水弹术 | 10% 眩晕 / 冻结 / 灼烧 |
| | 心魔 · 执念化形 | 30% | 藤蔓术 | 中毒 |
| | 天劫化身 / 仙人残念 | 40% / 45% | 雷音引 | 伐灾(自身) |
| | 狼王 · 赤瞳 | 20% | 藤蔓术 / 火球术 | 中毒 / 灼烧 |
| | 黑风寨主 · 屠九 | 25% | 落石术 | 10% 眩晕 |
| | 沧溟蛟 · 苍溟 | 30% | 水弹术 | 10% 冻结 |
| `EVENT_FOE_ELEMENT` | 妖兽首领 | 15% | 藤蔓术 | 中毒 |
| | 首席弟子 | 25% | 剑气诀 | —（无属性，不加） |
| | 井底灵蛟 / 守园老龟 | 15% / 25% | 水弹术 | 10% 冻结 |
| | 冰蛟 | 20% | 凝霜诀 | 10% 冻结 |
| | 火脉元灵 / 受伤毕方 | 25% / 15% | 火球术 | 灼烧 |
| | 天机傀儡 / 雷池元灵 / 雷池之灵 | 20% / 25% / 20% | 金刃术 / 雷音引 | 伐灾(自身，不影响玩家) |
| | 石门机关 | 15% | 落石术 | 10% 眩晕 |
| | 虚空兽潮 | 30% | 全部 5 个黄阶 | 五机制随机 |

**早期难度评估**：黄级秘境与早期事件的主要对手（匪首 / 石门机关 / 井底灵蛟 / 妖兽首领 / 受伤毕方）
施法率 15%~25%，命中后为「10% 概率一回合无法行动」或「1 层 DoT（当前血 10%，1 回合即退）」，
量级温和；**伐灾不会落到玩家身上**（BOSS 分支不读 `disaster`）。结论：可观但可控，无需门控。

---

## 7. 测试计划

### 新增 / 改造用例

| 套件 | 用例 | 断言要点 |
|---|---|---|
| `11-dead-config` | 「新机制④」扩展 | `dotCapByGrade('黄') === 1`（保留玄2/地4/天8） |
| | 新增：黄阶法术挂载机制（数据守卫） | 8 条法术字段逐一断言；`jianqi` **必须无** `stun/dotBurn/dotPoison/disaster` |
| | 新增：黄阶灼烧上限 1 | 火球术连施 2 次 → `b.dotBurn` 仍为 1 |
| | 新增：黄阶冻结确定性命中 | stub 沙箱 `Math.random = 0` → `b.stunNext === true`，日志含「冻结」 |
| | 新增：黄阶伐灾净化 | 先挂 `pDotBurn/pDotPoison` → 金刃术施放后清零，`disasterStacks === 1` |
| | 改造：「新机制⑤」免控 | 黄(cap1) 1 层即可免控；玄(cap2) 2 层即可；地 仍为 3；无 cap 记录回落 3 |
| `12-boss-element` | `dotCapByGrade` 断言 | 补黄阶 |
| | 新增：敌人施放黄阶法术对玩家落效果（双向） | `luoshi`→`b.pStunNext`；`huoqiu`→`b.pDotBurn === 1`；`tengman`→`b.pDotPoison === 1` |
| | 复查：以 `jinren` 作「纯伤害参照」的 BOSS 法伤用例 | 伐灾不进 boss 分支，实跑确认仍绿 |
| `02-engine-sim` | 全程模拟平衡回归 | 早期敌人变强后仍能推进，不变量校验通过 |

### 非空转验证（必做）

1. 临时删掉 `js/data.js` 那 8 行的机制字段 → 数据守卫用例应转红。
2. 临时把 `dotCapByGrade` 黄阶改回非 1 / 把免控门槛改回固定 3 → 对应用例应转红。
3. 记录回退前后的通过数差（预期 241/242 → 转红项）。

---

## 8. 文档同步清单

| 文档 | 改动 |
|---|---|
| `DEDAO_法术效果全等级大表.md` | ① 状态头；② **第二节档位表填上黄阶那 5 格「—」**；③ 第三节黄阶表「附加效果」列补机制；④ 第四节数量/机制覆盖表；⑤ 新增「黄阶机制档位」说明 |
| `DEDAO_法术新机制方案.md` | 注明机制线由「五机制 × 三阶（15）」扩为「× 四阶（20，含黄）」 |
| `DEDAO_秘境功法法术池映射.md` | **无需改动**（不新增法术 id，黄级秘境池不变） |
| `js/engine.js` 头注（机制口径注释） | 补黄阶档位与「免控按阶取小」说明 |
| `AGENTS.md` | 新增变更日志条目（第 50 条） |

---

## 9. 交付清单（执行时按序）

1. `js/data.js` 8 行 + `desc`；`js/engine.js` 2 处（`dotCapByGrade`、伐灾 cap/门槛）。
2. 测试新增与改造（第 7 节），先跑主仓库全量。
3. 非空转验证（回退 → 确认转红 → 还原）。
4. 文档同步（第 8 节）+ `AGENTS.md` 变更日志。
5. bump `index.html` / `index_pc.html` 的 `?v=`、`sw.js` 的 `CACHE`。
6. 同步两份 dist（`git show HEAD:<file>` 取**已提交版**，避免带入并发会话在途改动）。
7. 主仓库 + `dist/DEDAO_release` + `dist/taptap/dedao` 三跑测试。
8. commit / push（如遇并发会话在改同一文件，按 hunk 精准暂存；必要时用独立 worktree 验证 HEAD）。

---

## 10. 风险与备选

| # | 风险 | 说明 | 备选 |
|---|---|---|---|
| 1 | **黄阶「元素 buff + 机制」双挂，功能强于玄阶纯机制法术** | 例：火球术 = atkUp+12%/3 **且** 灼烧；玄阶烈火焚只有灼烧（但 dmg 2.0 > 1.5、上限 2 > 1）。定位不同、不算严格倒挂，但体感上黄阶「更全能」 | 若认为倒挂：给玄/地/天 机制法术也补回元素 buff（扩范围，改动量翻倍） |
| 2 | 敌人双向生效 → 早期难度上升 | 见第 6 节评估：量级温和 | 加 `noEnemyFx` 门控（用户已否决） |
| 3 | 御火诀 / 凝霜诀 / 雷音引 / 剑气诀 商店可购 | 花小钱即得机制，可能削弱上玄阶秘境刷法术的动力 | 若需收紧：把这几个从商店下架或提价 |
| 4 | 免控「按阶取小」使黄/玄 免控可达 | 金系 / 伐灾强度上升（黄1层、玄2层即可抵消一次控制） | 若嫌强：门槛改为 `min(3, cap)` 但要求 「达到上限的满层」才可触发（黄/玄 需一次性叠满） |
| 5 | 并发会话同时在改仓库 | 交付时可能撞车 | 按 hunk 精准暂存 + 独立 worktree 验证 |
