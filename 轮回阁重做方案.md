# 轮回阁重做方案

> 基于 T1.5 更新计划，删除功法/剑相关天赋，全面适配六维属性+命格系统。

---

## 一、设计原则

1. **删除**：`tech0`（家传心法）、`sword0`（祖传宝剑）
2. **新增**：六维补齐项 + 命格槽位扩展 + 命格效果加成
3. **保留**：通用成长项（灵石/寿元/修炼/种植等）
4. **总原则**：轮回天赋作为"局外成长"，补足命格系统的短板，而非直接替代命格

---

## 二、新轮回天赋表

### 2.1 六维属性类（每项 cost:3, max:5）

| id | 名称 | 描述 | 效果 |
|----|------|------|------|
| `wu` | 慧根 | 悟性 +1（先天资质） | `apply: { wu: 1 }` |
| `ti` | 强体 | 体魄 +1（肉身根基） | `apply: { ti: 1 }` |
| `dun` | 灵步 | 遁速 +1（身法根基） | `apply: { dun: 1 }` |
| `shen` | 神念 | 神识 +1（感知根基） | `apply: { shen: 1 }` |
| `dao` | 定心 | 道心 +1（心境根基） | `apply: { dao: 1 }` |
| `fu` | 招财 | 福源 +1（气运根基） | `apply: { fu: 1 }` |

### 2.2 命格强化类（每项 cost:5, max:3）

| id | 名称 | 描述 | 效果 |
|----|------|------|------|
| `destiny_slot` | 命格栏 | 初始命格栏位 +1 | `apply: { destinySlot: 1 }` |

### 2.3 通用成长类（cost:2-6, max:1-5）

| id | 名称 | 描述 | cost | max | 效果 |
|----|------|------|------|-----|------|
| `stone` | 殷实 | 出生时灵石 +1000 | 2 | 4 | `apply: { stone: 1000 }` |
| `juling0` | 见面礼 | 出生时自带聚气丹 ×3 | 1 | 3 | `apply: { elixirs: { juling: 3 } }` |
| `cult` | 道种 | 修炼速度 +10%（永驻） | 6 | 5 | `apply: { cultMul: 0.10 }` |
| `life20` | 延寿 | 出生寿元 +20 | 2 | 3 | `apply: { life: 20 }` |
| `shesheng` | 舍生 | 修炼速度 +10%，每次修炼 -1寿元 | 5 | 3 | `apply: { shesheng: 0.10 }` |
| `alchemy` | 丹心 | 炼丹时间 -1年 | 3 | 3 | `apply: { alchemyTimeReduce: 1 }` |
| `forge` | 器魂 | 炼器时间 -1年 | 3 | 3 | `apply: { forgeTimeReduce: 1 }` |
| `lvling_bottle` | 小绿瓶 | 灵草成长时间 -1年 | 3 | 3 | `apply: { herbGrowReduce: 1 }` |
| `extra_field` | 随身灵田 | 初始灵田 +1块 | 3 | 3 | `apply: { extraField: 1 }` |

---

## 三、代码变更

### 3.1 data.js — REINCARNATION 常量

```javascript
/* ---------------- 轮回天赋（局外成长） ---------------- */
const REINCARNATION = [
  /* === 六维属性 === */
  { id: 'wu',   name: '慧根', desc: '悟性 +1',   cost: 3, max: 5, apply: { wu: 1 } },
  { id: 'ti',   name: '强体', desc: '体魄 +1',   cost: 3, max: 5, apply: { ti: 1 } },
  { id: 'dun',  name: '灵步', desc: '遁速 +1',   cost: 3, max: 5, apply: { dun: 1 } },
  { id: 'shen', name: '神念', desc: '神识 +1',   cost: 3, max: 5, apply: { shen: 1 } },
  { id: 'dao',  name: '定心', desc: '道心 +1',   cost: 3, max: 5, apply: { dao: 1 } },
  { id: 'fu',   name: '招财', desc: '福源 +1',   cost: 3, max: 5, apply: { fu: 1 } },

  /* === 命格强化 === */
  { id: 'destiny_slot',  name: '命格栏', desc: '初始命格栏位 +1',         cost: 5, max: 3, apply: { destinySlot: 1 } },

  /* === 通用成长 === */
  { id: 'stone',         name: '殷实',   desc: '出生灵石 +1000',          cost: 2, max: 4, apply: { stone: 1000 } },
  { id: 'juling0',       name: '见面礼', desc: '出生聚气丹 ×3',           cost: 1, max: 3, apply: { elixirs: { juling: 3 } } },
  { id: 'cult',          name: '道种',   desc: '修炼速度 +10%',            cost: 6, max: 5, apply: { cultMul: 0.10 } },
  { id: 'life20',        name: '延寿',   desc: '出生寿元 +20',             cost: 2, max: 3, apply: { life: 20 } },
  { id: 'shesheng',      name: '舍生',   desc: '修炼速度 +10%，-1寿元/次', cost: 5, max: 3, apply: { shesheng: 0.10 } },
  { id: 'alchemy',       name: '丹心',   desc: '炼丹时间 -1年',            cost: 3, max: 3, apply: { alchemyTimeReduce: 1 } },
  { id: 'forge',         name: '器魂',   desc: '炼器时间 -1年',            cost: 3, max: 3, apply: { forgeTimeReduce: 1 } },
  { id: 'lvling_bottle', name: '小绿瓶', desc: '灵草成长时间 -1年',        cost: 3, max: 3, apply: { herbGrowReduce: 1 } },
  { id: 'extra_field',   name: '随身灵田', desc: '初始灵田 +1块',          cost: 3, max: 3, apply: { extraField: 1 } }
];
```

### 3.2 engine.js — applyReinc() 修改

```javascript
function applyReinc(s, meta) {
  // 六维属性
  s.wu  = (s.wu  || 0) + (meta.reinc.wu  || 0);
  s.ti  = (s.ti  || 0) + (meta.reinc.ti  || 0);
  s.dun = (s.dun || 0) + (meta.reinc.dun || 0);
  s.shen= (s.shen|| 0) + (meta.reinc.shen|| 0);
  s.dao = (s.dao || 0) + (meta.reinc.dao || 0);
  s.fu  = (s.fu  || 0) + (meta.reinc.fu  || 0);

  // 通用效果
  s.reinc.cult = meta.reinc.cult || 0;
  s.reinc.alchemyTimeReduce = meta.reinc.alchemy || 0;
  s.reinc.forgeTimeReduce = meta.reinc.forge || 0;
  s.reinc.shesheng = meta.reinc.shesheng || 0;
  s.reinc.herbGrowReduce = meta.reinc.lvling_bottle || 0;
  s.reinc.extraField = meta.reinc.extra_field || 0;

  // 命格栏初始化
  var baseSlots = 1 + (s.reinc.destinySlot || 0);
  if (!s.destinies) s.destinies = [];
  const list = REINCARNATION;
  list.forEach(function (r) {
    const n = meta.reinc[r.id] || 0;
    if (!n) return;
    for (let i = 0; i < n; i++) {
      if (r.id === 'stone')   s.stone += 1000;
      if (r.id === 'juling0') s.elixirs.juling = (s.elixirs.juling || 0) + 3;
      if (r.id === 'life20')  s.lifeMax += 20;
    }
  });

  // 命格栏初始化
  var baseSlots = 1 + (s.reinc.destinySlot || 0);
  if (!s.destinies) s.destinies = [];
  // 命格栏上限由境界+轮回决定，此处不截断，由UI展示时截断
}
```

### 3.3 engine.js — 命格效果函数（无需额外加成函数）

命格战斗效果直接使用 `getDestinyBonus()`，不再有轮回加成。
命格属性加值直接使用 `getDestinyAttrBonus()`，不再有轮回加成。

### 3.4 ui.js — 轮回阁渲染修改

无需修改渲染逻辑，数据驱动。只需确保 `REINCARNATION` 数组更新后，`renderRebirth()` 自动展示新内容。

### 3.5 engine.js — calcAttr 修改（命格属性受轮回加成）

```javascript
function calcAttr(s, attr) {
  let base = s[attr] || 0;
  let destiny = getDestinyAttrBonus(s, attr);  // 命格直接加值
  let talent = getTalentBonus(s, attr);
  let equip = getEquipBonus(s, attr);
  return (base + destiny + talent + equip) * getDestinyAttrMult(s, attr);
}
```

### 3.6 engine.js — 战斗属性计算修改

```javascript
function calcCombatAttr(s) {
  let atk = (calcAttr(s, 'wu') * 2 + calcAttr(s, 'shen') * 1.5) * getDestinyAttrMult(s, 'atk') + getEquipBonus(s, 'atk');
  let def = calcAttr(s, 'ti') * 0.5 * getDestinyAttrMult(s, 'def') + getEquipBonus(s, 'def');
  let critRate = calcAttr(s, 'shen') * 0.01 + getDestinyBonus(s, 'critRate');
  let dodgeRate = calcAttr(s, 'dun') * 0.005 + getDestinyBonus(s, 'dodgeRate');
  let extraAtkRate = calcAttr(s, 'dun') * 0.01 + getDestinyBonus(s, 'firstStrike');
  let lifesteal = getDestinyBonus(s, 'lifesteal');
  let thorns = getDestinyBonus(s, 'thorns');
  let counterRate = getDestinyBonus(s, 'counterRate');
  let executeBonus = getDestinyBonus(s, 'executeBonus');
  return { atk, def, critRate, critDmg: 2.0, dodgeRate, extraAtkRate, lifesteal, thorns, counterRate, executeBonus };
}
```

### 3.7 ui.js — 命格栏展示修改

```javascript
// 角色页命格栏位计算
function getDestinySlots(s) {
  var base = 1;  // 初始1格
  var reinc = (s.reinc && s.reinc.destinySlot) || 0;
  // 每突破大境界+1
  var bigRealm = bigIdxOf(s);
  return base + reinc + bigRealm;
}

// 命格装备上限
function canEquipDestiny(s) {
  return s.destinies.length < getDestinySlots(s);
}
```

---

## 四、轮回家成显示（ui.js）

```javascript
// 角色页轮回加成显示（修改现有逻辑）
function renderReincBonus(s) {
  var parts = [];
  // 六维
  var attrs = [
    ['wu','悟性'], ['ti','体魄'], ['dun','遁速'],
    ['shen','神识'], ['dao','道心'], ['fu','福源']
  ];
  attrs.forEach(function(a) {
    var v = s.reinc[a[0]] || 0;
    if (v > 0) parts.push(a[1] + '+' + v);
  });
  // 命格
  if ((s.reinc.destinySlot || 0) > 0) parts.push('命格栏+' + s.reinc.destinySlot);
  // 通用
  if ((s.reinc.cult || 0) > 0) parts.push('修炼+' + (s.reinc.cult * 10) + '%');
  if ((s.reinc.shesheng || 0) > 0) parts.push('舍生修炼+' + (s.reinc.shesheng * 10) + '%，-1寿元/次');
  if ((s.reinc.alchemyTimeReduce || 0) > 0) parts.push('炼丹-' + s.reinc.alchemyTimeReduce + '年');
  if ((s.reinc.forgeTimeReduce || 0) > 0) parts.push('炼器-' + s.reinc.forgeTimeReduce + '年');
  if ((s.reinc.herbGrowReduce || 0) > 0) parts.push('灵草-' + s.reinc.herbGrowReduce + '年');
  if ((s.reinc.extraField || 0) > 0) parts.push('灵田+' + s.reinc.extraField);
  return parts.length ? parts.join(' | ') : '无轮回加成';
}
```

---

## 五、修改文件清单

| 文件 | 修改内容 |
|------|----------|
| `js/data.js` | 替换 `REINCARNATION` 数组：删除 `tech0`/`sword0`/`wuxing_body`，新增 `dun`/`shen`/`dao`/`fu`/`destiny_slot` |
| `js/engine.js` | 修改 `applyReinc()`：新增六维属性初始化 + 命格强化字段；新增 `getDestinyBoostedBonus()`/`getDestinyBoostedAttrBonus()`；修改 `calcAttr()`/`calcCombatAttr()` 调用新函数 |
| `js/ui.js` | 修改 `renderReincBonus()` 展示命格强化项；修改角色页命格栏位计算 `getDestinySlots()` |

---

## 六、数值平衡

### 轮回点预算参考（总轮回点约50-80点/周目）

| 构建方向 | 推荐投入 | 效果 |
|----------|----------|------|
| 六维全修 | 慧根3+强体3+灵步3+神念3+定心3+招财3 = 18点 | 全六维+3 |
| 命格扩展 | 命格栏5×3 = 15点 | 命格栏+3 |
| 修炼速通 | 道种6+舍生5×3 = 21点 | 修炼速度+40%（含寿元代价） |
| 种植流 | 小绿瓶3×3+随身灵田3×3 = 18点 | 灵草-3年，灵田+3块 |
| 综合均衡 | 六维各1(6点)+命格栏5+道种6+延寿2×3 = 23点 | 基础六维+1，命格栏+1，修炼+10%，寿元+60 |

### 命格栏扩展验证

| 命格栏等级 | 总栏位（含境界加成） |
|------------|----------------------|
| 0级 | 1 + 0(轮回) + 0(境界) = 1格 |
| 1级 | 1 + 1(轮回) + 0(境界) = 2格 |
| 3级满 | 1 + 3(轮回) + 0(境界) = 4格 |
| 3级+元婴 | 1 + 3(轮回) + 4(境界) = 8格 |
