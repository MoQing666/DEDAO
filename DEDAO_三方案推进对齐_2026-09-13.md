# DEDAO 三方案推进对齐报告（2026-09-13）

> 目的：按用户指令「根据最新的法术和装备体系，推进」三份方案 —— `DEDAO_法宝系统设计_2026-09-08`、`DEDAO_轮回结算重做_方案`、`DEDAO_宗门商人重做方案`。
> 方法：逐份比对**设计文档**与**实际代码**（engine.js / data.js / ui.js，当前缓存 v94/dedao-v131，测试 194/194），并对照最新体系文档（`DEDAO_法术效果全等级大表.md` / `DEDAO_装备属性与装池完整表.md` / `DEDAO_法术新机制方案.md`）。
> 结论：**三套系统代码均已实装**，但三份文档状态标注与正文均严重过时、且与代码存在具体冲突。本报告给出对齐结论 + 冲突清单 + 下一步优先级。
>
> ⚠ **本文是 2026-09-13 的快照，其中数量为当时口径**（缓存 v94/dedao-v131、测试 194/194、法宝 **39** 件）。
> 截至 2026-09-15 已演变为：缓存 **v138/dedao-v176**、测试 **255/255**、法宝 **47** 件（仙阶 4 / 灵物 4）、
> 命格 46 个。表格里的旧数字**保持原样**（属历史记录），引用时请以 `js/data.js` 为准。

---

## 〇、总览核对

| 方案 | 文档标注状态 | 代码实际状态 | 推进结论 |
|---|---|---|---|
| 法宝系统设计 | 「规划稿，待确认后实装」 | **已实装**（39 件 ARTIFACTS 全在；机制改为「装备槽制」） | 文档状态/机制段需重写；战斗字段已全接通最新体系 |
| 轮回结算重做 | 「Plan / 拍板后执行」 | **已实装**（earnPoints/settlePoints/computeAdvPoints 全在；成就表已改；开荒池 bug 已修） | 公式与文档有偏差（渡劫分/endMul/exploreKills）；平衡表失真需重算 |
| 宗门商人重做 | 「已实装（76/76）」 | **已实装**（SECT_GOODS 29 件 + 游历池 9 件） | 仅 §二 内门法宝名写错（聚灵腰带→应为巨灵腰带）；功业翻倍语义未落地 |

---

## 一、DEDAO_法宝系统设计 —— 推进对齐

### 1.1 实际落地核对（已实装）

- **法宝全在** `data.js` 的 `ARTIFACTS`（本文写于 2026-09-13，当时 **39** 件；**2026-09-15 已增至 47 件**，
  其中仙阶 4 / 灵物 4 —— 成就 `fabao_da` 的文案同步改为「拥有全部四十七件法宝」，并由
  `test/09-achievements.test.js` 动态守卫）。字段经 `artifactStats(s)` 全量消费。
- **架构已重构为「装备槽制」**（设计文档 §2.2「遍历 `s.arts`」已过时）：
  - 法宝获得 → `applyOps` 的 `art` 分支（engine.js:931）调 `equipTreasureAuto` **自动装备**进 `s.equip.treasure`；旧档在 `startLife` 迁移（engine.js:55-61）自动迁入装备槽。
  - 容量 `maxTreasure(s)` = `3 + bigIdx + min(3,⌊道心/10⌋) + min(3,⌊神识/10⌋) + 轮回阁·先天灵宝`（engine.js:972）。即 炼气3 / 筑基4 / 金丹5 / 元婴6。
  - **`artifactStats` 只聚合已装备槽 `s.equip.treasure`；库存 `s.arts` 中的法宝不生效**（engine.js:306-311 注释明确）。
- **战斗字段全部接通最新战斗公式**：
  | 法宝字段 | 消费位置 | 最新体系对齐 |
  |---|---|---|
  | `critPct` | `getCritRate`（engine.js:707） | 与装备 `critPct` 同口径 ✓ |
  | `dodgePct` | `getDodgeRate`（709） | 与装备 `dodgePct` 同口径 ✓ |
  | `atkPct` / `scale.atk` / `lowHpAtk` / `daoAtkPct` | `calcAtk`（424-429） | 攻速/攻击百分比 ✓ |
  | `defToAtk` | `calcAtk`（428，棘鳞甲） | 防御转攻击 ✓ |
  | `stealPct` | `getRecoverPct`+`playerHit`（1619） | 吸血回血 ✓ |
  | `def`（flat） | `getDefense`（727，含 `s.artDef`） | 玄武龟甲/磐石铠/不灭金身 ✓ |
  | `defPct` | `getDefensePct`（730，封顶90%） | 金精甲 ✓（与 AGENTS.md §五 减伤封顶90% 一致） |
  | `hpMax` / `tiHpBonus` | `calcHpMax`（382/389） | ✓ |
  | 六维 `wu/ti/dun/shen/dao/ling` | `artAttr`→`effAttr`（546） | ✓ |
  | `cult` | `cultGain`（478） | ✓ |
  | `modeBonus`/`cultTwice`/`duantiEff`/`duantiMax`/`craftEff`/`craftKind`/`farmEff`/`mineEff`/`stoneYearPct` | 各系统（craftStudy/doDuanti/duantiCap 等） | ✓ |

### 1.2 与最新法术 / 装备体系对齐

- **最新装备体系**（7 词缀 `atk/def/critPct/atkSpd/recover/hpPct/mpPct` + 法宝独立池）：法宝的 combat 字段（`critPct/dodgePct/defPct/atkPct/stealPct`）与战斗公式**完全同口径、无冲突**。法宝走独立 `s.equip.treasure` 槽，不与装备 4 槽混用，架构干净。✓
- **最新法术体系**（41 法术 + 五机制 `stun/dotBurn/dotPoison/disaster`）：法宝是「常驻属性」，法术 buff 走「战斗内临时增益」，二者**叠加取更高值+更长时长**（AGENTS.md §五），互不干扰。✓
- **唯一缺口（已决策：补）**：法宝无任何「攻速 / 额外攻击」类（`getExtraAtkChance` 通道），设计文档曾删「螳螂刃」（`extraAtkChance`）后无补。最新战斗公式有攻速属性但法宝不覆盖。\n**→ 用户决策：补一件攻速类法宝**，打通 `getExtraAtkChance`，提案如下（待实现）：\n  - 名称建议：`踏风履`（或 `流光环`/`疾风铃`）；type 辅，grade 地；effect `{ atkSpd: 15 }` → 经 `getExtraAtkChance` 提供 15% 额外攻击几率（与装备 `atkSpd` 词缀同通道叠加）。\n  - 出处：游历流动商贩法宝池（data.js:3685，price≈1500，minDepth 1）或宗门内门（与巨灵腰带同层级）。\n  - 关于「巨灵腰带」：现 `juling_yaodai` 给 `tiHpBonus:0.25`（体修生命+25%，唯一覆盖），**建议新增而非改巨灵腰带**，避免丢失体修词条；若坚持可由巨灵腰带兼带 `atkSpd`。\n  - 平衡：无风险（仅补覆盖空白，与装备词缀同口径）。

### 1.3 文档冲突清单（需修正）

| 位置 | 文档说法 | 代码实际 | 修正建议 |
|---|---|---|---|
| 顶部「状态」 | 规划稿，待确认后实装 | 已实装（装备槽制重构） | 改「已实装（2026-09-13 装备槽制重构，39 件全生效）」 |
| §2.2 机制 | `artifactStats` 遍历 `s.arts` | 遍历 `s.equip.treasure`（已装备槽） | 重写机制段 + 补 `maxTreasure` 容量公式 + 自动装备说明 |
| §5.2 #10 | 周天星盘 grade 写「天」 | 实际 `grade:'地'`（data.js:264） | 改「地」 |
| §2.1 字段表 | 列 `apBonus`（行动点） | 无法宝使用、artifactStats 也未消费 → 死字段 | 注明「暂未使用」或补一件行动点法宝 |
| §5.10 宗门法宝定价 | 巨灵腰带 `stoneFix:2000`/`gongye:220` | 实际 `coin:'stone'`、`stoneFix:1000`（2026-09-13 重平衡）、无 gongye | 已按用户规格落地（见变更日志 §6） |

### 1.4 下一步 punch-list

| 优先级 | 事项 | 风险 |
|---|---|---|
| P1 | 重写文档状态头 + §2.2 机制段（装备槽制 + 容量公式） | 低 |
| P1 | 修正周天星盘 grade（天→地） | 低 |
| P2 | 补「攻速/额外攻击」类法宝（打通 `getExtraAtkChance`）——**已决策：补**，提案见 §1.2 | 中（无风险，待实现） |
| P2 | `apBonus` 死字段：补一件行动点法宝 或 从文档字段表移除 | 低 |
| P3 | 同步 `tools/player_sim.js` 镜像（若覆盖法宝公式） | 低 |

---

## 二、DEDAO_轮回结算重做 —— 推进对齐

### 2.1 实际落地核对（已实装）

- `earnPoints` / `settlePoints` / `computeAdvPoints` 全在（engine.js:4236-4298）。breakdown 已扩为 `{realm, trib, death, explore, ach}`（4259-4273），与方案 §十.4 一致。✓
- **成就表已按方案改**（data.js:2799-2875）：新增 7 项（初探秘境/秘境收藏家/遗世寻仙/悟道飞升/百家之长/长生久视/散修成道），`mo_yuan`（镇魔渊）已删。✓
- **开荒池 bug 已修**（方案 §十.5）：`reincTalentUpgrade` 扣 `meta.points`（engine.js:4596），不再用从未赋值的 `s.reincPoints`。✓

### 2.2 与文档 / 最新体系的偏差

| 项 | 文档（方案 §二/§九/§十） | 代码实际 | 影响 |
|---|---|---|---|
| 渡劫分 | `min(10, floor(broken/3))` 或 `floor(s.broken/3)` | `tribPassed * 3`（AGENTS.md §十八 已定稿） | 文档公式过时；实际每渡劫 +3 |
| 结局倍率 | §二 未包含 | 实际加 `endMul`：飞升×1.2 / 打破轮回×1.5（engine.js:4293，AGENTS.md §十六 确认） | 文档缺此乘子 |
| 秘境 per-kill | §五 明令「砍掉一切战斗收益 / 绝不奖励击杀场次」 | 实际 `computeAdvPoints` 加 `exploreKills*0.05`（engine.js:4248，封顶 adv cap） | **用户确认：正常，保留**（视为探索近似收益，非纯战斗奖励） |
| 平衡表 | §九（6/18/34/55/129） | 实际因 `tribPassed*3`+`endMul` 数值更高（飞升 jie0 实测≈164 而非 129） | 表已失真，需重算 |

- 其余完全符合方案意图：境界档（2/4/7/11/25）、死劫分 `deathPassed*2`、秘境单段分（2/4/7/11/15）与境界档对齐、广度阈值、单世封顶 `realm*2`、高劫加成 `K=0.05` 且删 `jie×2`、成就不翻倍。✓

### 2.3 文档冲突清单（需修正）

| 位置 | 文档说法 | 代码实际 | 修正建议 |
|---|---|---|---|
| §二 公式 | `(境+渡+死+秘+成)×(1+jie×0.2)` | 实际再乘 `endMul` | 补 `×endMul` 乘子 |
| §二/§十 渡劫分 | `min(10, floor(broken/3))` | `tribPassed*3` | 改公式 |
| §五「砍战斗收益」 | 禁击杀奖励 | `exploreKills*0.05` 仍在 | **已决策：保留**（用户确认正常，文档补注「探索近似收益」） |
| §九 平衡表 | 6/18/34/55/129 | 实测更高 | 用实际公式重跑 `reinc_validate.js` 重算 |

### 2.4 下一步 punch-list

| 优先级 | 事项 | 决策需求 |
|---|---|---|
| P1 | `exploreKills*0.05`：用户确认保留（探索近似收益，非纯战斗奖励） | 已决策 ✓ |
| P1 | 重算 §九 平衡表（实际公式 + `reinc_validate.js`） | 否 |
| P2 | 文档补 `endMul`、改渡劫分公式 | 否 |
| P3 | 确认 `reinc_validate.js` / `reinc_sim.js` 是否已用实际公式（否则重跑） | 否 |

---

## 三、DEDAO_宗门商人重做方案 —— 推进对齐

### 3.1 实际落地核对（已实装）

- `SECT_GOODS`（data.js:3644-3683）7 类 **29 件**（文档写 30，差 1 为文档笔误）；`ART_SHOP_ITEMS` 游历池 9 件（3686-3696）均落地。
- 定价语义：法宝灵石 = `GRADE_STONE[grade]` 中值（不翻倍），但 4 件宗门法宝已改用 `stoneFix` 锁定目标价（使 grade 可独立控制稀有度标签与秘境掉落阶位）。均 `coin:'stone'`（巨灵腰带1000 / 锻骨池1800 / 淬神台2000 / 九转金丹炉6000）。
- `sectBuy` / `buyStock` 各 kind 分支、`effTxt` UI 均按方案落地；rankMin 过滤不变。✓

### 3.2 偏差

| 项 | 文档（§二） | 代码实际 | 影响 |
|---|---|---|---|
| 内门法宝名 | 「聚灵腰带」 | 实际 `juling_yaodai`（**巨灵腰带**，data.js:288）；`聚灵腰带` 不存在于 `ARTIFACTS` | **文档错误**，与法宝设计文档 §5.10（巨灵腰带）一致 |
| 法宝功业价 | 列功业价（如 巨灵腰带 300=2×150 翻倍） | 4 件法宝均 `coin:'stone'`、`stoneFix` 锁灵石价、无 `gongye` → 实付灵石-only | **已决策**：法宝实付灵石（stoneFix），不混功业；文档 §二 功业价列按此更正 |

- 其余 6 类（功法/遁术/阵法/丹药/灵材/装备）与文档一致。✓
- 游历池 9 件（`ART_SHOP_ITEMS`）与法宝设计文档 §5.10 完全一致（含 `shiting_yuehua` 降价至 6000）。✓

### 3.3 文档冲突清单（需修正）

| 位置 | 文档说法 | 代码实际 | 修正建议 |
|---|---|---|---|
| §二 内门 | 聚灵腰带 | 巨灵腰带 `juling_yaodai` | 改名（聚灵腰带不存在） |
| §二 法宝功业列 | 有 gongye 价 | 代码无 gongye（灵石-only） | 决策后统一文档/代码 |
| 顶部「合计 30 件」 | 30 | 实际 29 | 改 29（或补 1 件） |

### 3.4 下一步 punch-list

| 优先级 | 事项 | 决策需求 |
|---|---|---|
| P1 | 修正 §二 内门法宝名（聚灵腰带→巨灵腰带） | 否（明确错误） |
| P1 | 决策法宝「功业翻倍」是否落地：改代码加 `gongye` 或改文档 | **需用户拍板** |
| P3 | 合计件数 30→29 | 否 |

---

## 四、跨方案一致性 & AGENTS.md 修正建议

- **AGENTS.md §十一 / §十八 关于法宝的描述与代码相反，会误导后续开发**（高风险）：
  - §十一：「法宝存于 `s.arts`……自动生效（储物袋获得即计入，无需手动装备）」「装备型法宝存 `s.equip.treasure`（容量 = bigIdx+1，最大 4）」——实际：`s.arts` 是**未生效库存**，生效的是 `s.equip.treasure`（容量 3+bigIdx+…，炼气3/元婴6）。
  - §十八：「`s.arts` 法宝自动生效，角色法宝栏须读 `s.arts`（不要误读恒空的 `S.equip.treasure`）」——**正好说反**：应读 `s.equip.treasure`，且它**非空**。
  - 建议：按本报告 1.1 的「装备槽制」口径重写这两处。
- 三份文档状态头均需刷新（法宝/轮回标「已实装·待文档对齐」；宗门标「已实装·有小错」）。

---

## 五、优先级总 punch-list（汇总）

| 优先级 | 事项 | 归属 | 风险 | 决策需求 |
|---|---|---|---|---|
| **P1** | 修正 AGENTS.md §十一/§十八 法宝存储描述（说反，误导风险高） | 跨方案 | 低 | 否 |
| **P1** | 宗门商人 §二 内门法宝 聚灵腰带→巨灵腰带 | 宗门 | 低 | 否 |
| **P1** | 法宝文档：状态头改「已实装」+ §2.2 重写装备槽制 + 周天星盘 grade 天→地 | 法宝 | 低 | 否 |
| **P1** | 轮回：`exploreKills*0.05` 用户确认保留（探索近似收益，非纯战斗奖励） | 轮回 | 低 | 已决策 ✓ |
| **P1** | 轮回：用实际公式重算 §九 平衡表 | 轮回 | 低 | 否 |
| **P2** | 轮回：文档补 `endMul`、改渡劫分公式 | 轮回 | 低 | 否 |
| **P2** | 宗门：决策法宝功业翻倍是否落地（代码加 gongye / 文档改说明） | 宗门 | 中 | **需拍板** |
| **P2** | 法宝：补攻速类法宝（打通 `getExtraAtkChance`）——**已决策：补**，提案见 §1.2 | 法宝 | 中 | 已决策 ✓（待实现） |
| **P3** | 法宝：`apBonus` 死字段处置 | 法宝 | 低 | 否 |
| **P3** | 宗门：合计件数 30→29 | 宗门 | 低 | 否 |

> 说明：本报告仅做**核对与对齐**，未改动三份源方案文档正文（避免就定价语义等未决项传播假设）。P1 项均为可立即执行、无歧义的低风险修正；标「需拍板」项请确认后我再落地代码/文档。

---

## 六、2026-09-13 实装变更日志（已落地代码 + 文档）

按用户「是」指令推进。改动文件：`js/data.js`、`js/engine.js`、`js/ui.js`、`test/automated/01-static-data.test.js`、`test/automated/02-engine-sim.test.js`；版本 `?v=94→95` / `dedao-v131→v132`；两份 dist 已同步；测试 **195/195 全绿**（主版本 + dist 副本均通过）。

### 6.1 法宝效率字段分离（doDuanti / 宗门练神峰·聚灵潭）
- **新增** `duantiShenEff`（淬神效率）：法宝 `淬神台` 的 effect 由 `duantiEff` 改为 `duantiShenEff:0.5`。
- `artifactStats` 聚合 `duantiShenEff`；`sectTrain`（宗门 神识/灵力 锤炼）消费它，神识/灵力 +1 改为 `+(1+duantiShenEff)`（淬神台在时 +1.5）。
- 保留 `duantiEff`（锻体效率）仅作用于 `doDuanti` 的 体魄+遁速（锻骨池）。
- 效果：**锻骨池（体魄+遁速）与 淬神台（神识+灵力）彻底分离**，各管一条训练轴；新增回归测试 `02-engine-sim`「法宝效率分离」验证（含反向确认 锻骨池 不影响宗门神识/灵力）。

### 6.2 4 件宗门法宝重平衡（保留 key 不破坏存档）

| 法宝 | 原 name/grade/价 | 新 name/grade/价 | effect |
|---|---|---|---|
| 锻骨池 | 锻骨鞭/守/地/1400 | 锻骨池/辅/玄/stoneFix 1800 | duantiEff:0.5（体魄+遁速） |
| 淬神台 | 淬神台/辅/天/4000 | 淬神台/辅/地/stoneFix 2000 | duantiShenEff:0.5（神识+灵力） |
| 九转金丹炉 | 九转金丹炉/辅/仙/8000 | 九转金丹炉/辅/天/stoneFix 6000 | duantiMax:5（不变） |
| 巨灵腰带 | 巨灵腰带/守/地/1400 | 巨灵腰带/守/玄/stoneFix 1000 | tiHpBonus 0.25→0.50 |

- 价用 `stoneFix` 锁定（grade 仅控制稀有度标签与秘境 BOSS 掉落阶位）；grade 下调使这些法宝更早可于对应阶位秘境 BOSS 掉落（按 `BOSS_TREASURE_BAND`）。
- ⚠️ **仙阶法宝现剩 4 件**（九转金丹炉 仙→天）：成就「仙器满堂」描述已由「五件」改为「四件」（逻辑 `xianqi_man:xianAll` 动态计数，仍可达）。

### 6.3 旧「秘境探索」战斗掉落入口（已弃用·根因）
- **数据**：`data.js` `EVENTS.mijing` 中事件含 `fight:{name,atk,hp,loot:{art:'...'}}`（守墓尸傀/冰蛟/守灯尸修/雷池元灵/荒神残念 等，均非空、loot 法宝均存在于 ARTIFACTS）。
- **运行时（已弃用）**：`Engine.explore(s)`（engine.js:3172 加权抽 `EVENTS.mijing`）→ 返回事件 → UI `choose(c)`（`if(c.fight) openBattle(...)`）→ 胜后 `applyOps(s, b.loot)` 自动装备。
- ⚠️ **根因**：秘境【探索】按钮（`btn-explore`）现走「秘境竖版地图」系统（`ADVENTURE_CONFIG` DAG），里面的「秘地探查」节点只给探索度/造化，**从不调用 `Engine.explore`**。故 `EVENTS.mijing` 全部战斗事件成为**孤儿事件、永不可见**——这正是「秘境探索没有遭遇战事件」的根因。

### 6.4 山河探索（游历子选项，新增 · 替代上述孤儿事件）
- **事件池** `EVENTS.shanhe`（data.js:1438 注册空数组）：**13 件** = 5 个原 mijing 战斗 BOSS + 踏风履战斗事件 + **7 个非战斗事件**（灵泉淬体/古观访道/古洞奇珍/散修遗泽/灵药幽谷/残碑参悟/山民相助）。
- **5 个掉宝 BOSS 迁移**：`baigu_gumu / han_feng_tan / wangu_dong / leichi / huangshen_tan` 由 `E('mijing',` 改挂 `E('shanhe',`（loot 法宝不变：幽魂披肩/玄武龟甲/喋血刃/棘鳞甲/磐石铠）。
- **新增攻速法宝 踏风履**（辅/地/atkSpd:10）：`shanhe_tafeng`（风灵兽 combat）掉落；因 地阶<仙，亦随 地阶秘境 BOSS 经 `BOSS_TREASURE_BAND` 随机掉落（满足「放入秘境掉落」）。
- **非战斗事件（7 件，持续扩充）**：灵泉淬体/古观访道/古洞奇珍（灵石+灵草/**道心+1**）/散修遗泽（灵石/**灵力+0.5**）/灵药幽谷（灵草/气血+修为）/残碑参悟（**道心+1**/以血换神识+1·气血上限-100）/山民相助（灵石/**道心+1**）——提供「其他选项」。
  - 注：2026-09-13 按用户校正，残碑参悟/古洞奇珍/散修遗泽/山民相助 等原「悟性」收益改「道心/灵力」口径（道心=真实属性 `s.dao`，`applyOps` 已支持）。
- **频率**：`Engine.shanheExplore(s)` 独立池加权抽 3 桩际遇（`openEventChoice` 三选一），**每年上限 1 次**（`SHANHE_CFG.perYearMax=1`，用 `s.shanheYearCount`，`newYear` 重置；达上限返回提示字符串「今年山河已探遍…」）。
- **UI** `renderTravel` 新增「山河探索」节点（⛰️），`runEvent` 副标题 `山河 · 探幽`；达上限时按钮点击仅 `log` 提示、不消耗行动点。
- **攻速通道接通**：`artifactStats` 聚合 `atkSpd`；`getExtraAtkChance` 加 `+ artifactStats(s).atkSpd/100`（与装备 `atkSpd` 同单位·百分点）；`artEffectText` 显示「攻速+N%」；`player_sim.js` 镜像同步。
- 版本 `?v=96→97` / `dedao-v133→v134`；两份 dist 同步；测试 **197/197 全绿**（主版本 + dist 副本，含新增「踏风履攻速」「山河探索池」「每年一次上限」回归测试）。
- 2026-09-13 修正：残碑参悟/古洞奇珍/散修遗泽/山民相助 4 件非战斗事件的收益由「悟性」口径改「道心/灵力」（`dao:1` / `ling:0.5` / `hpMax:-100`），`data.js` 已改；版本 `?v=97→98` / `dedao-v134→v135`；两份 dist 同步；测试 **197/197 全绿**。


### 6.5 手机端秘境体验专项 + 敌人掉落下调（2026-09-13 · 第二轮）

| # | 用户反馈 | 根因（已定位） | 处置 |
|---|---|---|---|
| 1 | 残魂考验难度 = 精英战 | 旧实现用「玩家 `atk×0.8` / 玩家 `hpMax×0.6`」——挂玩家缩放，会随玩家变强而水涨船高，且与秘境其余敌人（固定基线 × 深度 × 系数）**不同源** | 改走 `Engine.enemyGen(S,'elite',S.adv.depth)`（精英系数 ×1.4，清空 `loot`），名称/台词保留 |
| 2 | 初入秘境灵力条不是满的 | `startAdventure` 注释即「进入秘境不再回满血蓝」：入口沿用**俗世残蓝**，而 `combatStart` 战前只 +75% 灵力（封顶）→ 首战永远不满、法术开不出来 | `refreshStats` 后置 `s.mp = s.mpMax`（**只回灵力**；气血沿用进入时状态，是设计内容） |
| 3 | 手机端秘境页行数过多过密、地图只显示 4 行 | **`min-width:200px` 换行根因**：`.adv-vitals / .adv-right` 各 200px 在 360~414px 屏装不下 → `flex-wrap` 把 HUD 从 2 行撑成 4 行（+空丹药行共 ~126px），且行高 88px → 640px 屏仅剩 ~4 行地图 | ①窄屏把两列 `min-width` 归零（HUD 回 2 行）；②`chapter-overlay` padding 18→10、`chapter-head` margin 16→8、隐藏空丹药提示行；③行高 88→70、节点高 56→46（JS+CSS 同步） → 可见 **4 → 6 行** |
| 4 | 秘境探索与战斗都要「仙魔浩劫」BGM | 秘境无专属曲（`BGM_FILES.adventure` 指向 `bgm_main`），战斗固定播 `battle` | `audio.js` 新增 `xianmo` 键 + `BGM_FALLBACK.xianmo='battle'`；`doStartAdv` 进秘境即播、`openBattle` 在秘境中改用 `xianmo`，出秘境/试炼回 `game`。⚠ **缺 `assets/audio/bgm/bgm_xianmo.mp3`，需美术投放**（缺文件时自动回落 `bgm_battle.mp3`） |
| 5 | 手机端战斗只见血蓝数值、看不到红蓝进度条 | 窄屏下 `.hp-row .who`(64px) + `.hp-num`(70px) 固定宽度把 `flex:1` 的进度条挤到 **~7px 甚至 0**；且玩家血条原为绿色 `#4ec9a0`，与"红蓝"预期不符 | 血条 `#e8443e`（红）/ 灵条 `#3a86d6`（蓝）；窄屏 `who`→16px、`num`→56px、`.hp-bar` 加 `min-width:40px` → 360px 屏下进度条宽 **~75px**，清晰可见 |
| 6 | 敌人灵石掉落过高 | 单场 `(10 + 10·ed) × (1+bi·0.6)`：天级封顶层 **588/场**，远超同期其它系统（坊市丹药 40 / 事件 120~260 / 宗门法宝 1000~8000） | 改 `(5 + 5·ed) × realmM`（**整体减半**：黄首层 10 / 天封顶层 294）；精英 ×1.5、Boss ×2、高阶更肥的**层级关系保留** |

- 新增 3 条回归测试（`04-adventure`）：初入秘境灵力回满（并反向确认气血不回满）/ 残魂考验=精英战（源码级断言「不得残留玩家 atk×0.8 / hpMax×0.6」）/ 灵石掉落量级与层级关系。
- 版本 `?v=98→99` / `dedao-v135→136`；两份 dist（`DEDAO_release` / `taptap/dedao`）已同步；测试 **200/200 全绿**（主版本 + dist 副本）。
- 改动文件：`js/engine.js`、`js/ui.js`、`js/audio.js`、`css/style.css`、`index.html`、`index_pc.html`、`sw.js`、`test/automated/04-adventure.test.js` + 两份 dist + `AGENTS.md`。

### 6.6 第三轮：BGM 定稿 / 手游 HUD 资源位 / 地图 4 行视口 / 装备掉落禁越阶（2026-09-13）

| # | 用户反馈 / 要求 | 根因（已定位） | 处置 |
|---|---|---|---|
| 1 | `bgm_battle` 就是「仙魔浩劫」 | 上一轮误以为需要单独投放 `bgm_xianmo.mp3`，于是把 `BGM_FILES.xianmo` 指向该不存在文件、靠 `BGM_FALLBACK` 回落 `battle` —— **绕了一圈** | `BGM_FILES.xianmo = 'assets/audio/bgm/bgm_battle.mp3'`，`BGM_FALLBACK` 清空。**无需美术额外投放音频** |
| 2 | 手游版「行动 / 灵石」要在六维右边一上一下 | 两块资源块原本挤在 `header` 的 `.hud-actions` 内、与「名字 / 境界 / 命格」同一行；窄屏下把 header 顶成多行 | `index.html`：删掉 header 的 `.hud-actions`，六维包进 `.stats-top-row > .six-dim-wrap`，右侧加 `.action-info-col`（上=行动、下=灵石）。`style.css` 补基础规则 + 窄屏压扁。**PC 版 `index_pc.html` 保持原样**（仍 `.hud-actions`，由 `style_pc.css` 接管） |
| 3 | 秘境里「足足显示 9 行将来的道路，要求 4 行」 | 上一轮把行高压到 70px 后，`.adv-map` 仍是 `flex:1` **自适应高度** → 屏幕越高一次露出的层数越多（长屏实测 **9 行**）。行高压缩反而放大了露出行数 | `.adv-map` 改固定高度 `calc(var(--adv-visible-rows) × var(--adv-row-h) + var(--adv-node-h) + 10px)` = **336px**，另加 `max-height:62vh` 兜底；`ui.js` 新增 `ADV_VISIBLE_ROWS=4` 并把三个几何值写入根节点 CSS 变量（**行数口径单一来源**） |
| 4 | 黄级秘境掉出良品/上品「太离谱」，黄只能掉凡品和**高一级**产物 | `randomEquip` 里 `let tier = range[0] + rand(span+1)`（本来就在 `[1,2]` 内）**之后**又跟了一句 `if (rand<0.18 && range[1]<5) tier = range[1]+1` —— 这句**向上越阶**让黄级（`range=[1,2]`）掉出 tier3「上品」 | 删掉越阶语句；品质严格落在 `realmTierRange(bi)` 内。改用 `RANGE_BIAS_PER_DEPTH = 0.03` 的**深度偏置**（区间内偏向本阶上限：黄级首层 ~33% 出良品 → 深层 90% 出良品），**深度不再改变上限** |

**实测验证（无头 Edge，真实 CSS + 真实标记）**

| 项 | 验证方法 | 结果 |
|---|---|---|
| 手游 HUD 资源位 | 三点：在六维右侧 / 两块竖排 / 横向溢出元素数 | 360px：右侧 ✓ 竖排 ✓ 溢出 0 ｜ 390px：✓✓0 ｜ 414px：✓✓0 |
| 地图 4 行视口 | `#adv-map` 实测 `clientHeight` 与可见整行数 | 视口 ≥542px 高（640/720/858/1158 均测）→ `clientHeight = 336px` = **4.00 行**；`chapter-inner` 不溢出、说明/撤离按钮在视口内。视口 438px 高（超矮）→ `62vh` 兜底收缩到 272px（3.09 行），属预期的安全降级 |
| 装备掉落上限 | 4 个阶位 × 400 次抽样（首层 + 深 20 层） | 黄 `[1,2]` / 玄 `[2,3]` / 地 `[3,4]` / 天 `[4,5]`，**越阶 0 次** |

**补充说明**
- §6.5 第 3 行写的「可见 4 → 6 行」已被本轮推翻：行高压缩只解决"HUD 占屏"，**露出行数须由 `.adv-map` 固定高度控制**，设计口径是 **4 行**。
- 装备**完整掉落映射**（秘境阶位 → tier → 品阶 → 具体装备名 / 掉落概率 / 深度偏置 / 子类权重）已写入 `DEDAO_装备属性与装池完整表.md` **§六**，并同时在 §二/§七 标注「掉落区间」与「可穿戴区间」的区别。
- 版本 `?v=99→100` / `dedao-v136→137`；两份 dist（`DEDAO_release` / `taptap/dedao`）已同步（ui/css/engine md5 三份一致）；测试 **202/202 全绿**（主版本 + dist 副本）。
- 改动文件：`js/engine.js`、`js/ui.js`、`js/audio.js`、`css/style.css`、`index.html`、`sw.js`、`test/automated/03-ui.test.js`、`test/automated/04-adventure.test.js` + 两份 dist + `AGENTS.md` + `DEDAO_装备属性与装池完整表.md`。

---

## 七、2026-09-15 闭环进度（本报告 §五 punch-list 逐项处置）

> 本轮逐项核实了 §五 汇总清单。**结论：P1 全部闭环；P2 一项已实现、一项待拍板；P3 全部闭环。**

| 优先级 | 事项 | 状态 | 处置 |
|---|---|---|---|
| P1 | 修正 `AGENTS.md` §十一/§十八 法宝存储描述（说反） | ✅ **已闭环** | §十一/§十八 主体（"`s.equip.treasure` 才生效、`s.arts` 是不生效库存"）早前已修对；本轮补修 §十八 残留的容量旧值 `bigIdx+1` → `maxTreasure` 实式（炼气3/筑基4/金丹5/元婴6，最大 9） |
| P1 | 宗门商人 §二 内门法宝 聚灵腰带→巨灵腰带 | ✅ **已闭环** | 源方案文档实测**已是**「巨灵腰带」（本报告所指的错误在当前版本中不存在，属更早修订已修） |
| P1 | 法宝文档：状态头改「已实装」+ §2.2 重写装备槽制 + 周天星盘 grade 天→地 | ✅ **已闭环** | 状态头与 §2.2 已对齐「装备槽制」；`zhoutian_xingpan` 在 `data.js:244` 实为 `grade:'地'`，文档 §5.2 #10 亦已为「地」。本轮另补：标题「含 39 法宝池」→ 标注当前 **47**；字段表 `apBonus` 补注「通道已通但无数据源」 |
| P1 | 轮回：用实际公式重算 §九 平衡表 | ✅ **已闭环（并挖出更大问题）** | 见下「重大发现」 |
| P2 | 轮回：文档补 `endMul`、改渡劫分公式 | ✅ **已闭环** | §二 补 `endMul`；渡劫分 `⌊渡劫次数/3⌋` → `min(10, 渡劫次数×3)`，并明确**不得用 `s.broken`** |
| P2 | 宗门：决策法宝「功业翻倍」是否落地 | ⏳ **待用户拍板** | 已在 `DEDAO_宗门商人重做方案.md` 顶部加 ⚠ 分歧说明：代码 4 件法宝均 `coin:'stone'`+`stoneFix`、**无 `gongye`**，玩家实付纯灵石。要落地就改代码，要放弃就改表，**不要只改文档** |
| P2 | 法宝：补攻速类法宝（打通 `getExtraAtkChance`） | ✅ **已实现** | `tafeng_lv` **踏风履**（辅/地/`atkSpd:10`），山河古径「踏风古径」风灵兽掉落；`artifactStats` 聚合 `atkSpd`、`getExtraAtkChance` 已接通 |
| P3 | 法宝：`apBonus` 死字段处置 | ✅ **已闭环（按"注明暂未使用"处理）** | 字段表补注：通道已通（`Engine.actionPoints` 已 `+artifactStats(s).apBonus`）但 47 件法宝无一提供 → 实际恒为 0，视作预留字段 |
| P3 | 宗门：合计件数 30→29 | ✅ **已闭环** | `SECT_GOODS` 实测 **29**，源方案文档已改 29 并注明原写 30 为笔误 |

### 重大发现：`tools/reinc_validate.js` 自身与引擎分叉（一张错误镜像污染四张表）

跑 P1「重算 §九 平衡表」时发现，**脚本自己就是错的** —— 三处分叉同时存在：

| # | 脚本写法 | 引擎实际 | 后果 |
|---|---|---|---|
| ① | `Math.floor(s.broken / 3)` | `Math.min(10, s.tribPassed * 3)`（engine.js:4730） | `s.broken` 是**突破次数**，`s.tribPassed` 才是**渡劫次数**（至多 3）。引擎里有同源前车之鉴（成就 `sanjie` 曾误用 `s.broken`，engine.js:4801） |
| ② | 无 | `endMul`：飞升/仙 **1.2**、打破轮回 **1.5**（engine.js:4740） | 飞升偏低约 27% |
| ③ | 漏 | `+ Math.round(exploreKills * 0.05)`（engine.js:4696） | 秘境击杀分丢失 |

**净效应**：脚本输出的 `jie0` 列 `6/18/34/55/129` **整列错误**（正确 **`6/18/37/61/164`**），
而 `DEDAO_轮回结算重做_方案.md` 的 **§7.1 / §7.2 / §7.3 / §九 四张表**全部照抄它。
更隐蔽的是脚本自己那条「全部天赋全满 = 1406」断言**长期报 ❌**（实测 **1296**），因不在 `run.js` 视野里而无人看见。

**处置（两层）**：
1. 脚本内新增**测试7**「读 `DEDAO_轮回结算重做_方案.md` 断言表内数字」→ 文档与实算**绑死**；
2. 新增 **`test/automated/13-tools-reinc.test.js`**（断言脚本 exit 0 且含 `0 失败`）→ 纳入常规测试视野。

详见 `AGENTS.md` 变更日志 **#59**。四张表已按实算重排（含 §7.3 加满局数由新 jie0/jie9 重算）。

### 后续（同日第二轮）：镜像问题不止 `reinc_validate.js` 一个

按上面的判据回头扫 `tools/`，发现同一套错法在**三个脚本里各抄了一遍**：

| 脚本 | 分叉 | 后果 |
|---|---|---|
| `reinc_points.js` | 同款 `floor(s.broken/3)` + **残留已删除的 `jie×2` 平加** + 漏 `deathPts`/`advPts`/`endMul`；`REINC_TALENT` 成本抄成 `0/10/20/40/80`（真值 `0/6/12/18/24`） | 「本世合计」整列不可用 |
| `reinc_sim.js` | 六维 `max:5`（真值 9）→ `sixTotal` 360（真值 1080）；同款 `broken/3` | 加满局数整表错 |
| `player_sim.js` | 标签写「全天赋 1406」（真值 1296） | 报告标题印错（计算本身正确） |

**处置：不再镜像，去真加载引擎。** 新增 `tools/_engine_loader.js`（vm 沙箱拼一个脚本加载
`js/data.js` + `js/engine.js`），三个脚本改为数据表直读、公式直调 `Engine.settlePoints`/`earnPoints`。
改完 `tools/` **零公式镜像**，且 `reinc_sim.js` 与 `reinc_validate.js` 两条独立实现互证同一组 jie0。

另：`13` 套件新增第 2 条用例，对 `tools/*.js` 做**陈旧写法静态扫描**（黑名单 + 必须走 loader）。

> 顺带一个平衡结论：文档本表用的「保守成就集」（2/4/9/14/48）低于引擎实际发放的成就分
> （同进度 6/11/22/37/63），故 §一/§二 引用的 `jie0`（6/18/37/61/164）属**偏低估计**，
> 实为 **10/25/50/84/182**。详见 `AGENTS.md` 变更日志 #60。
