# 进入页面重做方案 v2

> 参考异世轮回录，命格抽取+轮回选择整合到进入页面，轮回塔保留在主页面。

---

## 一、核心规则

### 1.1 轮回塔
- **保留在主页面**，是局外养成的核心
- 玩家在轮回塔中消耗轮回点购买天赋
- 天赋影响进入页面的抽取数量、可选命格数、锁定机制等

### 1.2 命格抽取
- **抽取数量** = 3 + 天赋`extra_destiny`等级（不受劫数影响）
- **可选数量** = 1 + 天赋`destiny_slot`等级 + 劫数加成
  - 凡尘/1-2劫：选1个
  - 3劫及以上：+1额外选择
  - 极限：3个命格（destiny_slot=1 + 3劫）
- **命格池品质**：
  - 凡尘/1-2劫：白~紫（不含金）
  - 3劫及以上：白~金（解锁仙命）

### 1.3 锁定机制
- **锁定天赋**：1级，消耗12轮回点，允许锁定1个命格后重新抽取
- **锁定槽位**：
  - 默认：1个锁定槽
  - 6劫及以上：+1锁定槽（共2个）
- **锁定规则**：
  - 锁定的命格在重新抽取时保留
  - 锁定不额外消耗轮回点
  - 重新抽取消耗1轮回点

### 1.4 轮回选择
- 初始只显示【凡尘】
- 使用 +/- 按钮切换不同劫
- 显示：劫数名称 + 是否解锁 + 是否解锁新内容
- **不显示**具体难度数值

---

## 二、进入页面UI布局

### 2.1 整体结构

```
┌─────────────────────────────────────┐
│           得道 · 天命抉择           │
├─────────────────────────────────────┤
│  轮回 · 凡尘                        │
│  [ - ]                    [ + ]     │
├─────────────────────────────────────┤
│  天命抉择 · 抽取3选1               │
│  ┌─────┐ ┌─────┐ ┌─────┐          │
│  │ 🔒  │ │ 命格2 │ │ 命格3 │     │
│  │ 锁定 │ │ 选择  │ │ 选择  │     │
│  └─────┘ └─────┘ └─────┘          │
│  [ 重新抽取 (1点) ]                 │
├─────────────────────────────────────┤
│  你的名号                           │
│  ┌─────────────────────────┐       │
│  │     张三丰              │       │
│  └─────────────────────────┘       │
│  [ 随机取名 ]                       │
├─────────────────────────────────────┤
│  ┌─────────────────────────┐       │
│  │      开始这一世         │       │
│  └─────────────────────────┘       │
│  [ 返回山门 ]                       │
└─────────────────────────────────────┘
```

### 2.2 轮回选择区域

**布局：** 中央显示当前劫名，左右 +/- 按钮切换

**显示内容：**
- 当前劫数名称（如"凡尘"、"初劫"、"二劫"）
- 解锁状态：已解锁 / 未解锁
- 新内容提示：解锁新命格 / 解锁新锁定槽

**切换逻辑：**
- 点击 `+` 切换到更高劫数
- 点击 `-` 切换到更低劫数
- 未解锁的劫数显示灰色，不可选择

**示例：**
```
轮回 · 凡尘
[ - ]                    [ + ]

凡尘 · 默认轮回
✅ 已解锁
```

```
轮回 · 三劫
[ - ]                    [ + ]

三劫 · 需完成2劫轮回
🔒 未解锁
🎁 解锁：额外命格选择+1
```

### 2.3 命格抽取区域

**标题：** `天命抉择 · 抽取{N}选{M}`
- N = 抽取数量（3 + extra_destiny，固定）
- M = 可选数量（1 + destiny_slot + 劫数加成）

**卡牌设计：**
```
┌─────────────────┐
│  🔒             │  ← 锁定按钮（右上角）
│  【白】铜皮铁骨  │  ← 品质颜色 + 名称
│                 │
│  肉身强健，     │  ← 描述
│  不易受伤       │
│                 │
│  体魄 +1        │  ← 属性加成（绿色）
│  防御 +3%       │  ← 战斗特效（蓝色）
└─────────────────┘
```

**品质颜色：**
- 白（凡命）：`#b0b0bc`
- 绿（本命）：`#4ec9a0`
- 蓝（奇命）：`#5ac8fa`
- 紫（极命）：`#b26de0`

**交互：**
- 点击卡牌：选择/取消选择
- 点击锁定按钮：锁定/解锁该命格
- 已选卡牌：边框高亮 + 选择标记
- 已锁命格：锁标记 + 不可取消选择

**锁定槽显示：**
- 显示当前锁定槽数量
- 已锁定的命格显示锁标记

**重新抽取：**
- 按钮：`[ 重新抽取 (1点) ]`
- 消耗1轮回点
- 锁定的命格保留，其余重新抽取

### 2.4 起名区域

**布局：** 输入框 + 随机按钮

**内容：**
- 输入框：默认显示随机名字
- 随机按钮：点击生成新随机名字

---

## 三、标题页保持不变

```
得道
DEDAO
凡尘一梦 · 轮回修仙

[ 开始轮回 ]  ← 进入进入页面
[ 继续征途 ]  ← 有存档时显示
[ 存档 · 读档 ]
[ 轮回塔（天赋） ]  ← 保持不变

文字修仙 · 人生模拟 · 一世又一世
```

---

## 四、新轮回天赋

### 4.1 锁定天赋

```javascript
{ id: 'destiny_lock', name: '天命锁定', desc: '锁定1个命格后重新抽取', cost: 12, max: 1, apply: { destinyLock: 1 } }
```

### 4.2 天赋效果汇总

| 天赋ID | 名称 | 效果 | 影响 |
|--------|------|------|------|
| extra_destiny | 大千命格 | 抽取数量+1 | 每级多抽1个 |
| destiny_slot | 我命由我 | 可选数量+1 | 每级多选1个 |
| destiny_lock | 天命锁定 | 解锁锁定机制 | 允许锁定后重抽 |

---

## 五、劫数与内容解锁

| 劫数 | 解锁条件 | 新内容 |
|------|----------|--------|
| 凡尘 | 默认 | 基础命格池（白~紫） |
| 初劫 | 完成1次0劫 | — |
| 二劫 | 完成1次1劫 | — |
| 三劫 | 完成1次2劫 | +1额外选择 + 解锁仙命 |
| 四劫 | 完成1次3劫 | — |
| 五劫 | 完成1次4劫 | — |
| 六劫 | 完成1次5劫 | +1锁定槽 |
| 七劫 | 完成1次6劫 | — |
| 八劫 | 完成1次7劫 | — |
| 九劫 | 完成1次8劫 | — |

---

## 六、代码修改清单

| 文件 | 修改内容 | 优先级 |
|------|----------|--------|
| `index.html` | 新增 `screen-enter` 页面结构 | P0 |
| `css/style.css` | 新增进入页面样式 | P0 |
| `js/data.js` | 新增 `destiny_lock` 天赋 | P0 |
| `js/ui.js` | 新增 `showEnterPage()` 函数 | P0 |
| `js/ui.js` | 新增 `renderDestinyPool()` 函数 | P0 |
| `js/ui.js` | 新增 `lockDestiny()` / `unlockDestiny()` 函数 | P0 |
| `js/ui.js` | 新增 `rerollDestiny()` 函数 | P0 |
| `js/ui.js` | 修改 `startNewLife()` 从进入页面获取数据 | P0 |
| `js/engine.js` | 新增 `destinyLock` 到 `applyReinc()` | P1 |

---

## 七、抽取算法

```javascript
function rollDestinyPool(count, jieLevel) {
  // count: 抽取数量（3 + extra_destiny）
  // jieLevel: 当前劫数（0=凡尘, 1=初劫, ...）
  const pool = [];
  
  // 命格池权重（根据劫数）
  let weights;
  if (jieLevel >= 3) {
    // 3劫+：解锁仙命
    weights = { '白': 40, '绿': 30, '蓝': 20, '紫': 8, '金': 2 };
  } else {
    // 凡尘/1-2劫：白~紫
    weights = { '白': 50, '绿': 30, '蓝': 15, '紫': 5 };
  }
  
  // 抽取count个命格
  for (let i = 0; i < count; i++) {
    // 根据权重随机选择品质
    const grade = weightedRandom(weights);
    // 从该品质中随机选择一个命格
    const destiny = pickRandomFromGrade(grade);
    pool.push(destiny);
  }
  return pool;
}
```

---

## 八、锁定机制流程

```
1. 抽取命格池 → 显示3-5张命格
2. 玩家选择命格（点击卡牌）
3. 玩家锁定命格（点击锁按钮）
4. 玩家点击"重新抽取"
5. 消耗1轮回点
6. 锁定的命格保留
7. 其余位置重新抽取
8. 更新显示
```

---

## 九、测试要点

1. **新玩家：** 无轮回记录，凡尘轮回，抽取3选1，白~紫命格
2. **3劫玩家：** 抽取3选2，命格池含仙命（金）
3. **锁定机制：** 锁定命格后重新抽取，锁定的命格保留
4. **劫数切换：** +/- 按钮切换劫数，显示解锁状态
5. **命格选择：** 选择数量达到上限后不可再选
6. **重新抽取：** 消耗轮回点，更新命格池
7. **开始游戏：** 获取选中的劫数、命格、名字，开始游戏
