# PLAN — 宗门入宗考验改造 & 商人货币重划 & 删除宗门阵法商品

> 日期：2026-09-08 · 状态：待评审 · 关联 AGENTS.md §十二(宗门) §十三(游历)
> 目标文件：`js/data.js` / `js/engine.js` / `js/ui.js` / `test/automated/01·02·03`
> 前序基线：全量自动化测试 **76/76 通过**（未提交改动已含本轮大重构，此为增量方案）

---

## 〇、现状诊断（已核实）

存在 **3 套重复且互相矛盾的"拜入宗门"路径**，且「入宗考验」结果**从未写入玩家地位**：

| # | 入口 | 触发 | 问题 |
|---|---|---|---|
| 1 | MAINLINE `ml_2_0`「拜入仙门」(data.js:777) | idx2 练气后期 | 硬编码拜入青云剑宗，**无宗门三选一、无考验**；后续 idx3/4 主线(初入宗门/大比/秘境)假设已入宗 |
| 2 | 宗门页 `renderSect`「!S.sect」分支 (ui.js:6038) | 手动点选宗门卡 | 直接 `S.sect=xx; S.sectRank='杂役'`，**无考验** |
| 3 | `sectJoinFlow` (ui.js:1841) | 突破筑基后若 `!S.sect` 自动触发 | 三仙门择一，**只设 sect 不设 rank**，与 #1/#2 叠加冲突 |

**已核实的事实**
- 「入宗考验」`Engine.sectTrial`(engine.js:2984) 存在，但 `sectDoTrial`(ui.js:6083) **只把评定结果显示在弹窗，从不写回 `S.sectRank`**。
- 天然门禁已内建：`sectGoods`(engine.js:3073) 在 `!s.sectRank` 时返回 `[]`；`sectBuy`/`tryRankUp`/`commissionAvailable` 均要求 `sectRank` 存在。**只要通过考验前不设 `sectRank`，商人/任务/晋升自动受限**——UI 只需据此隐藏完整菜单即可。
- 状态默认干净：`startLife`(engine.js:640) 与 `loadState` 迁移补丁(engine.js:56-58, 2889) 均默认 `sect:null, sectRank:null, gongye:0`。
- MAINLINE 播放器(ui.js:1933-1998) 的 `effect` 仅走 `Engine.applyOps`，**无法直接写入自定义 `sectRank`** → 入宗考验需一条**专门播放分支**（而非纯 MAINLINE 数据）。
- `endYear` 结算有 `julingYearEnd`(engine.js:2670) 与宗门自动晋升 `tryRankUp`/`servantEscape`——杂役概念需厘清。
- 「一年可触发多剧情，在年初触发」：当前 `checkYearEvents`(engine.js:2712) 为**单事件 return**（死劫→主线→大比任选其一）。主线被大比/死劫抢占时会被顺延。

### 关于 array_juling（你问"是什么！！"）
- data.js:3062 `{ref:'array_juling'}` 实为宗门商店在售的「**宗门聚灵阵**」，属 **僵尸/残缺功能**：
  - 购买后仅设 `s.flags.arrayJuling=true`(engine.js:3095)，**游戏机制从不消费该 flag**（修炼加成读的是洞府 `s.array.juling.level`，见 engine.js:376）。
  - 同理 `array_wuxing`(3082)→`s.flags.arrayWuxing`(3096) 也是死 flag。
  - **结论：删除即可，无功能损失。** 有效的是**洞府**聚灵阵(screen-cultivate, ui.js:5934，读 `s.array.juling`)与**百艺页**五行阵(ui.js:5978，读 `s.array.wuxing`)，这两套**保留不动**。

---

## 一、改造总览

| 任务 | 说明 | 优先级 |
|---|---|---|
| **T1 删除宗门阵法商品（僵尸）** | 移除宗门商店的聚灵阵/五行阵两件商品及相关 flag 死代码 | P0（独立、低风险，先做） |
| **T2 入宗考验收口为考验剧情** | 新增「入宗考验」主线剧情 + 专门播放分支，统一接管三套入宗路径；未过考验受限 UI | P0（核心） |
| **T3 商人货币按类型划分** | 全商品双货币 → 功法/遁术/法宝/装备=灵石；丹药/灵材=功业 | P1 |
| **T4 年初可触发多剧情** | 年初事件改为可排队连续触发（主线不被死劫/大比抢占） | P1（支撑 T2 的"下一年年初触发"） |
| **T5 测试同步 + AGENTS.md 刷新** | 补/改测试断言、跑 76+、更新项目宪法 | P0（门禁） |

---

## 二、T1 删除宗门阵法商品（僵尸代码）——先做，独立提交

**现状梳理**
| 位置 | 内容 | 处理 |
|---|---|---|
| data.js:3062 | `{ref:'array_juling',kind:'array',...}` | **删行** |
| data.js:3082 | `{ref:'array_wuxing',kind:'array',...}` | **删行** |
| engine.js:3094-3096 | `sectBuy` 内 `kind==='array'` 分支（设两 flag） | **删分支**（array 商品无来源后不可达） |
| ui.js:6198 | `sectDoShop` owned 三元链里两处 array 判空 | **删 array 分支**（回落默认 false） |

**改动后校验**
- data.js:3061-3070 区块头注释「§6.3 双货币」中"阵法"定价说明一并修正。
- `grep -rn "array_juling\|array_wuxing\|flags.arrayJuling\|flags.arrayWuxing\|kind === 'array'\|kind:'array'"` 全清。
- **保留**：data.js `JULING_ARRAY`/`WUXING_ARRAY`(2988-3010)、engine 洞府聚灵阵(2908-2937)、ui 洞府/百艺阵法 UI——这些属洞府/百艺系统。
- 缓存版本 +1：`sw.js CACHE v74→v75`；`index.html js?v=33→34`。
- 跑测试套件。

---

## 三、T2 入宗考验收口（核心）

### 3.1 状态门禁约定
- **未过考验**：`S.sect` 可空或已选，但 `S.sectRank = null`（初始即如此）。
- **已过考验**：`sectRank` 落定 `真传/内门/外门/杂役`。
- UI 判定函数（ui.js 内新增）：`sectPassed() = !!(S.sect && S.sectRank)`。

### 3.2 三套旧路径统一到考验
| 旧入口 | 处置 |
|---|---|
| `ml_2_0`「拜入仙门」(data.js:777) | **改写**：不再硬编码青云宗、不再直接入宗，改为**引导语/提示"若要拜入仙门，须先赴宗门应考"**，或直接作为考验入口的引子。具体形态由剧情稿定 |
| `sectJoinFlow`(ui.js:1841) | **改**：突破筑基后 `!S.sect` 时不再即时三选一，而是触发/引导走「入宗考验」主线剧情 |
| `renderSect`「!S.sect」手动分支(ui.js:6038) | **改**：选宗门后**不再设 rank=杂役**，进入受限"应考"界面（见 3.3） |

### 3.3 宗门页两态 UI（ui.js `renderSect`）
**态 A — 未过考验（`!S.sectRank`）**：仅显示
- 宗门三选一（可选）
- 一张「入宗考验」卡片（展示三项标准：武骨 悟性≥8 / 道心 道心≥8 / 实战胜一场）
- 其余菜单（商人/任务/晋升/大比/练神/传功/切磋）**不渲染**
- 若选宗后考验**失败**：提示条件，允许原地重试或改投他门。

**态 B — 已过考验（`sectRank` 存在）**：现完整 8 项菜单（不变）。

### 3.4 考验执行与定级
- 复用 `Engine.sectTrial(S, win)`(engine.js:2984) 的评分 → **改为将 `r.rank` 写回 `S.sectRank`**（新增入参保证只写一次，或由调用方写）。
- `sectDoTrial`(ui.js:6083)：接受考验 → 实战(autoFight 宗门巡守)→ `sectTrial` 评 `真传/内门/外门/杂役` → **写 `S.sectRank`** → 存档 → `refresh`。
- 通过后 `renderSect` 切到态 B。

### 3.5 新增专门播放分支（年初触发）
由于 MAINLINE `effect` 不能写 `sectRank`，在 ui.js 年初处理(约 ui.js:2007 前)新增一个**独立的待触发标志**，优先级高于普通 mainline 且不与死劫冲突排队：
- engine.js `checkYearEvents`：当 `S.sectRank===null` 且年份/境界满足（**练气中期即 idx≥1**，按用户"练气中期后下一年年初"）且未 seen → 设 `S.pendingSectTrial=true`，但与现有 mainline/死劫**一并返回或按其优先级**。
- ui.js 年初：若 `pendingSectTrial` → 进入考验剧情播放（选宗→三项说明→实战→定级写 rank）。

### 3.6 支线剧情补写（可选内容层）
- 宗门三选一时各自的考验剧情差异（如青云重剑、丹霞重丹、玄天重稳），做成 choices 分支。
- 需新增 2-3 个考验相关章节文案（data.js 或剧情文件）。

---

## 四、T3 商人货币按类型划分

### 4.1 定价规则（data.js `SECT_GOODS` + ui/engine）
| 商品类型 | 货币 | 价格来源 |
|---|---|---|
| 功法 tech / 遁术 dun | **灵石** | `GRADE_STONE[grade]` 中值 |
| 法宝 art | **灵石** | `GRADE_STONE[grade]`（不翻倍） |
| 装备 equip | **灵石** | `EQUIPS[].price`（stoneFix） |
| 丹药 elixir | **功业** | `gongye`（显式） |
| 灵材 mat | **功业** | `gongye`（显式） |

> 阵法 array 已随 T1 删除，不再进入本表。

### 4.2 结构改动
- `SECT_GOODS` 每件加 `coin: 'stone'|'gongye'`，或按 kind 推导（推荐后者，少改数据、规则集中）。**推荐**在 engine 建 `sectGoodCoin(g)` 映射。
- 商品行只保留其对应币种价格字段（或双字段但 UI/结算只读所属币种）。
- `sectBuy`(engine.js:3081)：按 `coin` 判定扣灵石或功业，消息文案区分。
- `sectDoShop`(ui.js:6183)：卡片只显示所属币种价格，`owned` 判断沿用。

---

## 五、T4 年初多剧情排队（支撑 T2）

- `checkYearEvents`(engine.js:2712) 目前单 return。改为返回**一个事件队列**（`pendingEvents[]`），或新增"高优先级待触发"槽位，确保 `pendingSectTrial` 不会被大比/死劫抢占、且可与既有 mainline 在同一新年依次播放。
- ui.js 年初循环消费队列，逐个播放后再进当年正题。

---

## 六、T5 测试同步 & 宪法刷新

| 文件 | 加测 |
|---|---|
| test/automated/01 | 断言 SECT_GOODS 无 `array` kind / 无 array_juling·array_wuxing；每种商品恰好一个币种字段合法 |
| test/automated/02 | `sectBuy` 分币种扣减正确；`sectTrial` 写回 sectRank；`sectGoods` 未过考验返回 [] |
| test/automated/03 | renderSect 态A/态B 切换；商人只显示单一币种价格 |

- 跑 `node test/automated/run.js` 至全绿。
- 更新 AGENTS.md：§十二(宗门)并入宗考验门禁与新商人货币；刷新 sw/index 版本号记录。

---

## 七、执行顺序与提交建议

| 顺序 | 内容 | 提交信息 |
|---|---|---|
| 1 | T1 删宗门阵法商品（独立小改） | `删除宗门商店聚灵阵/五行阵僵尸商品与死flag` |
| 2 | T3 商人货币分型 | `宗门商人改为按类型分灵石/功业单货币` |
| 3 | T2+T4 入宗考验收口 + 年初多剧情 | `入宗考验主线剧情收口：未过考验仅受限UI，通过后按评分定级` |
| 4 | T5 测试 + 宪法 | `测试同步与AGENTS.md刷新` |

> 每步独立跑测试、独立提交，便于回滚。

---

## 八、待确认（开发前）

1. 「练气中期后下一年年初触发」：练气中期对应 idx（1 还是 2）？现 `ml_2_0` 在 idx2 触发。建议 idx≥2 且年≥N。
2. `ml_2_0`/`ml_2_1` 等既有"拜入仙门/初入宗门"主线条目：**改写**还是**删除**？（若统一走新考验剧情，旧硬编码青云宗条目建议改写为考验引导。）
3. 未过考验但选过宗门后，是否允许**改投他门**（重考）？
4. 宗门 `ml_3_0` 秘境/大比等假设"已入宗"，若玩家一直不过考验会卡剧情 → 考验是否**软强制**（可长期推后，但宗门内容不开放）。
