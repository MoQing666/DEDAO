# PLAN · 百艺「挖矿/种植」分化 + 秘境连锁解锁 2026-09-09

> 范围：Part I(百艺分化+行动栏) 与 Part II(秘境连锁解锁) —— **均已按定稿全量实施**，测试 86/86 通过（sw v81 / js v40）。本文件保留为设计与落点记录。

---

## 一、背景与诉求（用户原话 → 解析）

> 「百艺中补充挖矿，种植的分化设计有问题，境界分类和数量应该分开，这两步都先 plan」

按用户澄清确认的方向：

| 决策点 | 结论 |
|---|---|
| 核心目标形态 | **境界管等级（能种/挖的最高档位 黄→天），数量管规模（投入越大产出越大），两轴解耦、各自成长** |
| 数量轴（灵田） | **单亩播种株数**：同一块田一次可种多株，成熟后按株数翻倍产出，省去逐块操作 |
| 数量轴（挖矿） | **投入强度 / 次数**：投入行动点 + 气血越多，一次挖得越多；档位由矿脉深度决定 |
| digMine 处理 | **接入并重构为「境界(档位) + 深度(成长)」双轴**，废弃当前写死的「气血换铁」UI |
| 本次不重点改 | 灵田「亩数解锁块数」轴（`getMaxFields`/`unlockField`）保持现状，不并入本次 |

---

## 二、现状诊断（代码事实）

### 2.1 玩家真实触达路径
`btn-baiyi` → `showBaiyi()`(ui 4315) → `renderBaiyiPage()`(4320) + `renderCraftsExtra()`(5966)。
即百艺页实际 = **炼丹列表 + 炼器列表 + 灵田卡片 + 五艺研习 + 五行阵**。

### 2.2 关键代码事实与问题

| # | 位置 | 事实 | 问题判定 |
|---|---|---|---|
| 1 | ui 4180-4233 `renderArtsTab('mine')` | 挖矿 UI 只存在于**遗留 4-tab**（artTabs 4002/`renderArtsTab` 4020），**未被 baiyi 触达** | 玩家当前**挖不到矿**，挖矿缺席 |
| 2 | engine 2523 `digMine(s)` | 已实现「深度 depth(0→10) + 随机 灵石15%/伴生草15%/灵铁70% + 大境界决定档位 + `mineEff`加成」，并已在 engine 3215 导出 | **从未被 UI 调用**，深度系统闲置 |
| 3 | ui 4471 `plot.endYear` | renderBaiyiPage 用 `endYear` 判成熟 | **engine 实际写的是 `planted`(engine 2479/2481)**，字段不一致 → 收获判定恒「可收获」的隐性 bug |
| 4 | ui 4276-4313 `showPlantSelect` | 播种入口，`plantField(S,id,1)` **写死 quantity=1** | 「数量」维度缺失，无法多株播种 |
| 5 | data 300-309 `FIELD_SEEDS` + `FIELD_GRADE_MAP` | 黄/玄/地/天 4 种「灵草田」，`cost/years/gain/grade` 全耦合在 seed | 境界与数量无独立表达；播种消耗同等级灵草当「种子」语义怪 |
| 6 | engine 2486-2488 `getMaxFields` | 硬编码返回 `3`；`s.field` 初始空；`unlockField`(2489)花灵石扩容 | 亩数轴未随 `extra_field` 天赋 / 境界打通（**本次不处理**，仅记录） |
| 7 | data 269-279 `MATERIALS` | 材料已含 `herb_黄/玄/地/天`、`iron_黄/玄/地/天` 8 种分级 | 档位用「黄玄地天」分级已具备 |
| 8 | data 32/2509 | 大境界 idx：0炼气/1筑基/2金丹/3元婴；`needRealm` 0-3 对应黄→天 | 「境界管等级」的档位锚点已具备 |

### 2.3 数值锚点（供设计参考）
- `FIELD_SEEDS`（data 301-306）：播种需同等级灵草 `cost`，`gain` 为单株产出区间：

| seed | 档 | cost | years | gain(株) |
|---|---|---|---|---|
| lingshen_huang | 黄 | 1 | 1 | [3,6] |
| lingshen_xuan | 玄 | 3 | 2 | [4,8] |
| lingshen_di | 地 | 6 | 3 | [5,10] |
| lingshen_tian | 天 | 10 | 4 | [6,12] |

- `FORMULAS` 炼化消耗（data 286-297）：黄丹需 herb_huang×5、玄×10~15、地×20~30、天×40；铁料 iron_黄×15、玄×25、地、天×50。

---

## 三、设计目标（本次改造后应达成的形态）

### 3.1 总则：两轴解耦
- **等级轴 = 境界 + 矿脉/田 成长**：能种的最高档 = `min(当前可解锁档, 该亩田已养成的等级)`。
- **规模轴 = 玩家投入**：灵田 = 单亩播种株数 n；挖矿 = 单次投入（行动点 + 气血量）。

### 3.2 种植：境界管等级 · 株数管规模（已按用户最新决策更新）
- **档位门槛（用户定稿）**：灵草等级档 黄=炼气(0)、玄=筑基(1)、地=金丹(2)、天=元婴(3) —— 该门槛**只约束「在灵田买苗」**：境界不到，买不到高于自身境界的苗；
  但**若身上已备有高级灵草，可直接当苗下种，不受境界档限制**（自备草绕过买苗门槛）。
- **成本 = 灵石为主 + 灵草为辅**（用户定稿采纳）：买苗花灵石（主）+ 需少量同等级灵草作「辅料/催苗」；手头缺灵石时可用同等级灵草折抵部分灵石。避免旧「纯灵草种灵草」自循环卡死。
- **播种**：任一空闲灵田 → 选「档位(黄玄地天)」×「株数(1/3/6)」，一亩田存 `{seed, planted, qty:n}`。
- **产量**：成熟后产出 = `sum(gain 区间) × n × farmEff`，统一进对应档位 herb。
- **成熟判定**：统一改用 engine 的 `planted`/`needYears`（修掉 `endYear` 字段错乱）。

### 3.3 挖矿：境界管档位 · 深度管成长 · 投入管规模
- **档位**：由 `大境界 idx` 决定当前矿脉能出的铁/伴生草等级（炼气=黄铁…元婴=天铁），沿用现有数组映射。
- **成长**：保留并打通 `s.mine.depth`：随挖矿次数累计 depth(0→10)，提升单次保底与出灵石/伴生草概率——这就是「矿脉愈挖愈深」的养成第二轴。
- **投入**：替换遗留「气血换铁」两按钮 → 接入 `digMine(s, invest)`，`invest` 决定单次投入（行动点 1 + 气血若干），产出按档位×投入系数结算。
- **入口**：在 `renderBaiyiPage` 补「── 灵矿 ──」区块，展示当前矿脉档位 / 深度 / 已存材料，提供「挖掘」操作（1 行动点 + 气血）。

### 3.4 百艺等级（craft.lingkuang / craft.lingtian）的角色
建议作为**次级加成**而非主门槛：灵田/灵矿百艺等级提升 → 株数上限档位开放更高批量（如 lingtian Lv2 可 1 次种 5 株、Lv3 可 10 株）+ 微量 farmEff/mineEff；**大境界仍是第一等级门槛**。是否采用，见 §5 待定项。

---

## 四、改造落点（分层，按依赖顺序实施）

### 阶段 A：字段与机制修整（engine + data）
| 步 | 文件 | 改点 | 目的 |
|---|---|---|---|
| A1 | js/data.js | `FIELD_SEEDS` 增加 `realmMin`（黄0/玄1/地2/天3）、`costStone`(买苗灵石)、`costHerb`(辅料灵草) 字段 | 提供境界门槛与双成本数据 |
| A2 | js/engine.js | 拆 `plantField` 为两态：①`buySeed` 买苗(受 `realmMin` 限，灵石为主+灵草为辅)；②直接下种(自备草，无视 realmMin，仅扣灵草) → 统一写 `plot={seed,planted,qty}`（弃用 endYear） | 落实「买苗限境界/自备草可种」 |
| A3 | js/engine.js | `harvestField`/`fieldInfo`：按 `qty` 叠加随机产量 ×farmEff | 规模(株数)翻产出 |
| A4 | js/engine.js | 重构 `digMine(s, invest)`：保留 depth 成长，`invest`(气血/行动点)决定结算量 | 挖矿接入前提 |
| A5 | js/engine.js | 导出 `buySeed`/`plantField` 新签名 + `fieldPlantable`(可种/可买档位) 辅助 | UI 调用 |

### 阶段 A′：行动栏表述（用户定稿）
| 步 | 文件 | 改点 | 目的 |
|---|---|---|---|
| A′1 | index.html | `btn-explore` 文案 `秘境（2点）` → `秘境`（**仅此一处，其它按钮/入口不动**） | 行动栏只显示【秘境】，去掉行动点后缀 |

### 阶段 B：百艺页 UI（ui.js）
| 步 | 改点 | 目的 |
|---|---|---|
| B1 | 修 `renderBaiyiPage` 灵田段：成熟判定用 engine 字段；卡片显示 qty 与成熟进度 | 修字段错乱 |
| B2 | 重写 `showPlantSelect(fieldIdx)`：选「档位 × 株数(1/3/6)」→ 买苗或下种(见 A2) | 数量轴 + 买苗/下种落地 |
| B3 | 在 `renderBaiyiPage` 末尾新增「── 灵矿 ──」区块：显示档位/深度/库存，提供「挖掘(1行动点+气血)」调 `digMine(S,invest)` | 挖矿入口落地 |
| B4 | 移除/停用遗留 4-tab 挖矿写死逻辑（`renderArtsTab('mine')` 或改为复用 digMine），避免双实现 | 去重 |

### 阶段 C：测试 + 版本
| 步 | 文件 | 改点 |
|---|---|---|
| C1 | test/automated/02-engine-sim.test.js | 新增用例：多株播种产量叠加；境界门槛（炼气不可种玄级以上）；digMine 深度成长 + 档位随大境界；endYear 与 fieldInfo 成熟判定正确 |
| C2 | 版本/AGENTS | sw.css/js 缓存自增；AGENTS.md 同步 §版本号、§五/§十二百艺描述、变更条目；`node --check js/*.js` + 全量 82→N 通过 |

---

## 五、待用户拍板项（种植部分，其余已按定稿）
| # | 待定项 | 我的建议（默认） | 说明 |
|---|---|---|---|
| 1 | ~~株数档位~~ | ✅ **已定稿：1/3/6**；是否再随 lingtian 百艺等级开放更高批量(如10) | 见 §3.2 |
| 2 | ~~播种成本~~ | ✅ **已定稿：灵石为主 + 灵草为辅**；具体单株价格/折抵比例待细化 | 见 §3.2 |
| 3 | 灵矿投入上限与气血扣减 | 沿用「1 行动点 + 100~200 气血」量级，digMine 按投入放大 | 与现数值体系衔接 |
| 4 | 是否保留灵石/伴生灵草随机分支 | 保留（depth 高时灵石出率上升），玩家主要追求灵铁 | 趣味性 |
| 5 | lingkuang/lingtian 百艺等级是否作为次级批量门槛 | 采纳（见 3.4） | 若嫌复杂可下阶段再加 |

---

## 六、验收标准（Done 定义）
1. 玩家在百艺页能看到并操作「灵田 播种(可选档位×株数) / 采收」与「灵矿 挖掘」。
2. 炼气期只能种黄级、挖黄铁；筑基解锁玄；金丹地；元婴天（境界档位生效）。
3. 单亩播种 n 株 → 成熟产出 ≈ n 倍单株，farmEff 正确乘入。
4. 成熟判定正确（不再出现未熟即「可收获」）。
5. `digMine` 深度随次数累加，矿藏档位随大境界上升；UI 不再有两套写死挖矿。
6. 行动栏 `btn-explore` 文案 = `秘境`（无「（2点）」后缀），其余按钮不变。
7. 82/82 既有测试通过 + 新增用例全绿；版本缓存自增、AGENTS.md 同步。

---

# Part II · 秘境「连锁解锁」改造 plan（独立评审）

## 用户诉求（原话 → 解析）
> 「修改行动栏表述为【秘境】（按钮文案 秘境（2点）→ 秘境，其余不动）……新增机制，若战胜黄级秘境的BOSS，玄级秘境直接开放，同理战胜玄级则地级直接开放，秘境解锁有境界和探索度满并战胜BOSS两个解锁机制」

解析为两大动作：
1. **行动栏文案**：`秘境（2点）` → `秘境`（已并入 Part I 阶段 A′1，本章不再重复落点，仅作验收）。
2. **秘境解锁双通道**：
   - 通道①（原机制）**境界达标**：`realmReq` 满足即开（黄=炼气…天=元婴）。
   - 通道②（新机制）**通关前置连锁**：战胜本级秘境 BOSS（通关=当次探索度满100% + 胜BOSS，见现状），**持久记录**后 → 下一级「直接开放」，不再要求境界。

## 现状（代码事实）
| # | 位置 | 事实 | 问题 |
|---|---|---|---|
| 1 | engine 1306 `startAdventure` / ui 754 `openAdvSelect` | 进入门槛 = `bigIdxOf >= realmReq`（**仅境界**） | 无通关前置通道 |
| 2 | ui 997 `fightBoss` 胜利 | `S.adv.cleared = true` | **仅本次冒险会话内**，进入即建新 `s.adv`(engine 1322 cleared:false)，**无跨期/跨境界持久记录** |
| 3 | ui 954-990 / engine 1692 | 直面 BOSS 须探索度满 100%（`advCanFightBoss`） | 「胜BOSS」天然蕴含「当次探索度满」→ 通关证据可仅记 BOSS 胜 |
| 4 | data 2609-2656 `ADVENTURE_CONFIG` | 黄/玄/地/天/仙 五级；`realmReq` 0/1/2/3/3；`xian.isSpecial`(遗世仙踪，每10年一现) | 连锁链应止于天级，仙级保持特殊规则(待拍板) |
| 5 | ui 717-783 `openAdvSelect` | 卡片按 `canEnter=bi>=realmReq` 置灰禁用 | 需改为「境界 **或** 通关前置」任一满足即可点入 |

## 三、(Part II) 解锁模型设计
- **持久记录**：`s.flags.advClear`（或用独立 `s.advClears` 数组），`Boss 战胜瞬间写入`该级 key（huang/xuan/di/tian）。随存档持久（生死簿/轮回是否保留——见待拍板）。
- **解锁判定函数** `Engine.advUnlocked(s, key)`：
  ```
  unlock(key) = bi >= realmReq(key)                 // 通道①境界
                || advClear[prev(key)] === true      // 通道② 上一级已通关(黄级视为恒开 prev=null)
  ```
- **连锁方向（定稿）**：黄(恒开) → 玄(筑基 or 通黄) → 地(金丹 or 通玄) → 天(元婴 or 通地) → **仙·遗世仙踪(元婴 or 通天；纳入连锁后，通关天级亦可直接开放)**。
- **仙级(遗世仙踪) 定稿 = 纳入连锁**：`advUnlocked('xian')` 取「境界元婴 OR 通关天级」；其「每十年一现」的事件窗口规则与解锁判定**解耦**——是否可入遵循现有事件轮播(engine `isXianAdventureAvailable`)，是否解锁遵循本双通道模型。入口同时满足「事件现身」与「已解锁」才可进（衔接细节见待拍板注）。
- **UI 呈现**：`openAdvSelect` 每张卡显示当前是否解锁及解锁理由；已通关的上一级显示「已通关 → 已开启下一级」提示；未解锁显示「需 X 境界 或 通关 X 级秘境」。

## 四、(Part II) 改造落点
### 阶段 D：engine（解锁状态 + 写入）
| 步 | 改点 | 目的 |
|---|---|---|
| D1 | init/startLife 建 `s.flags.advClear={}`；**轮回/转世重开时清空（定稿：不随轮回保留，每世独立）**；同世内随存档持久 | 持久化容器 + 轮回策略 |
| D2 | 新增 `markAdvClear(s,key)`：BOSS 胜时置 `s.flags.advClear[key]=true`；胜点 = ui fightBoss 胜利分支(ui 997) 调用 | 写入通关记录 |
| D3 | 新增 `advUnlocked(s,key)`（境界 OR 前置通关，黄恒开，含仙级）；导出 | 供 UI/进入双重校验 |
| D4 | `startAdventure`(engine 1306)：门槛改为 `!advUnlocked(s,advKey)` 即拒绝；仙级额外叠加「事件现身」校验 | 服务端兜底 |

### 阶段 E：ui（选择界面 + 入口）
| 步 | 改点 | 目的 |
|---|---|---|
| E1 | `openAdvSelect`(ui 753)：`canEnter = Engine.advUnlocked(S,key)`；每卡显示解锁理由/已解锁提示；仙级卡片仍仅当事件现身时加入 | 双通道可视化 |
| E2 | `fightBoss` 胜利分支(ui 996-999) 调用 `Engine.markAdvClear(S, S.advType)`；通关结算文案补「已开启下一级秘境」（通关天级 → 提示仙级可直入） | 胜利即写解锁 |

### 阶段 F：测试 + 版本
| 步 | 改点 |
|---|---|
| F1 | 02-engine-sim 增：境界不足但通关黄级 → `advUnlocked('xuan')=true`；未通关且境界不足 → 仍锁；通关天级 → `advUnlocked('xian')=true`；markAdvClear 幂等；转世重建后 advClear 清空 |
| F2 | 03-ui 增（可选）：openAdvSelect 卡片 disabled 与解锁文案 |
| F3 | 版本缓存自增 + AGENTS.md 同步（版本号 / 秘境解锁规则 / 变更条目）；`node --check js/*.js` + 全量通过 |

## 五、(Part II) 决策定稿记录（用户已拍板）
| # | 决策点 | 定稿结论 |
|---|---|---|
| 6 | 通关记录是否随「轮回/转世」保留 | **不保留**：每世独立，转世重开后 `advClear` 清空，需重刷连锁 |
| 7 | 仙级(遗世仙踪)是否纳入连锁 | **纳入**：`advUnlocked('xian')`= 元婴境界 OR 通关天级；「每十年事件现身」仍独立判断、须叠加 |
| 8 | 「下一级直接开放」是否放开更多约束 | **只放开入口门槛**：不放开「一年一次」「行动点」，维持现状 |

> 注：仙级"每十年事件现身"与"双通道解锁"是两回事——须**事件现身且已解锁**才可进入；若通关天级但事件未到，卡片仍不渲染（对齐现有 `isXianAdventureAvailable`）。

## 六、(Part II) 验收标准（Done 定义）
1. 炼气玩家通关黄级 BOSS → 玄级卡片变可入（境界仍未筑基也能进玄级）。
2. 玄级 BOSS 胜 → 地级开放；地→天同理；**通关天级 → 仙级(事件现身时)可入**。
3. 未通关且境界不足时，对应级卡片仍置灰并显示「需境界 或 通关上级」。
4. 通关记录同世内持久（sl 重载仍解锁），且同档案一年内仍守「一年一次」；**转世重开后清空需重刷**。
5. 行动栏 `btn-explore` 文案为 `秘境`。
6. 全量测试通过 + 版本缓存自增、AGENTS.md 同步。
