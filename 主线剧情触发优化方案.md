# DEDAO 主线剧情触发优化方案

> 解决主线剧情触发突兀、与出生文案冲突、缺乏阶段总结等问题。

---

## 〇、当前问题分析

### 0.1 问题一：出生文案与主线剧情冲突

**当前流程：**
```
bg故事（"你出生在...道士来了..."）
  ↓
灵根天成章节（"你睁开眼，灵根是XXX"）
  ↓
destinySelect（命格选择）
  ↓
ml_0_0 凡人少年（"道士在你面前蹲下..."）
```

**问题：**
- bg故事已经讲了"道士来了"，ml_0_0又讲"道士搭脉"，两段叙事在时间线上重叠
- bg故事是纯文字叙述，没有选择权，玩家被动接受
- 命格选择插在中间，打断了叙事连贯性

### 0.2 问题二：主线剧情触发突兀

**当前触发方式：** 每年结束时自动检查，满足idx条件就触发下一个主线事件。

**问题：**
- 没有"这一年你做了什么"的过渡文案
- 从修炼/探索/战斗突然跳到主线剧情，缺乏叙事缓冲
- 玩家感觉主线是"被推着走"而不是"自然发生"

### 0.3 问题三：剧情结果重复

**已修复：** 删除了 `ev.result` 的重复 log。

---

## 一、优化方案

### 1.1 出生文案整合

**核心思路：** 将bg故事、灵根揭示、第一个主线事件合并为一个完整的开场叙事。

**新流程：**
```
命格选择（先选命格，确定命运基调）
  ↓
统一开场章节（bg + 灵根 + 道士 = 完整叙事）
  ↓
ml_0_1 灵根觉醒（道士画符）
  ↓
正式开始游戏
```

**开场章节文案（替换bg + ml_0_0）：**

```
第一章 · 凡尘旧事

你出生在一个偏僻的小村庄，父母早亡，由叔父抚养长大。

叔父是个老实巴交的庄稼人，对你视如己出。你在他膝下长大，虽不富裕，却也安稳。

直到十六岁那年——

一位云游道士路过村庄。他穿着洗得发白的青布道袍，背上斜挂一柄木剑，须发皆白，目光却锐利如鹰。

他在村口的老槐树下停住脚步，目光扫过一群嬉闹的孩童，最终落在你身上。

"过来。"他朝你招手。

你迟疑地走上前。他伸出两根枯瘦的手指搭在你腕上，闭目不语。

片刻后，他睁开眼，目光复杂。

"根骨不错。"他点头，从怀中取出一枚玉符递给你，"灵根已觉醒。从今日起，你便是修仙者了。"

你接过玉符，指尖触及冰凉的玉石，一股暖流涌入体内——那是天地灵气，无处不在。

"修仙之路，先修己身。"道士教你最基础的吐纳之法，"灵气入体，淬炼经脉。你已有灵根，剩下的，看你自己。"

【选择】
- 专心修炼（修为+100）
- 四处看看（道心+0.5）
```

**修改要点：**
1. bg故事不再单独显示，融入开场章节
2. ml_0_0 凡人少年和ml_0_1 灵根觉醒合并为一个事件
3. 开场章节末尾给出第一个选择，让玩家有参与感
4. 命格选择保留在开场之前（作为"命运的馈赠"）

### 1.2 主线触发过渡文案

**核心思路：** 在主线事件触发前，先显示"年度总结"，再自然过渡到主线剧情。

**实现方式：** 在 `endYear()` 中添加过渡文案逻辑。

**年度总结文案模板：**

```javascript
function getYearSummary(s) {
  const lines = [];
  const year = s.year;
  const age = s.age;
  
  // 基础时间描述
  if (s.cultedThisYear) {
    lines.push('这一年你闭关修炼，修为精进。');
  } else if (s.adv && s.adv.status === 'running') {
    lines.push('这一年你深入秘境探索，收获颇丰。');
  } else if (s.lastSocial) {
    lines.push('这一年你游历四方，见识了修仙世界的精彩与残酷。');
  } else {
    lines.push('这一年你四处游历，见识渐长。');
  }
  
  // 年龄感怀
  if (age === 20) lines.push('你已弱冠之年，修行之路才刚刚开始。');
  else if (age === 30) lines.push('而立之年，你已非昔日村中少年。');
  else if (age === 40) lines.push('不惑之年，你对修仙之道有了更深的理解。');
  else if (age === 50) lines.push('半百之龄，你已见过太多生死离别。');
  
  // 境界变化
  if (s.brokenThisYear) {
    const st = STAGES[s.idx];
    lines.push('你突破至' + st.realm + st.sub + '，实力大增。');
  }
  
  return lines;
}
```

**触发流程（修改后的endYear）：**

```javascript
// 修改endYear()的返回逻辑
function endYear(s) {
  // ... 现有逻辑 ...
  
  // 1. 先显示年度总结
  const yearLines = getYearSummary(s);
  if (yearLines.length) {
    showChapter('第' + s.year + '年 · ' + s.age + '岁', yearLines, {
      subtitle: '流年回顾',
      toLog: true
    }).then(function() {
      // 2. 年度总结后，再检查主线/事件触发
      checkMainlineAndEvents(s);
    });
  } else {
    // 无总结文案，直接检查触发
    checkMainlineAndEvents(s);
  }
}
```

### 1.3 主线事件叙事衔接

**核心思路：** 每个主线事件的开头，根据当前状态添加过渡文案。

**过渡文案规则：**

| 触发条件 | 过渡文案示例 |
|----------|------------|
| 刚突破境界 | "突破至{realm}后，你感觉天地间的灵气更加清晰了。" |
| 刚完成秘境 | "秘境归来，你正在整理收获，忽然……" |
| 刚加入宗门 | "入宗{N}年，你已逐渐适应了宗门的生活。" |
| 有道侣flag | "林婉儿来信说……" |
| 普通情况 | "这一日，你正在修炼，忽然感应到……" |

**实现方式：** 在MAINLINE事件数据中添加 `intro` 字段：

```javascript
{
  id: 'ml_2_1', idx: 3, title: '初入宗门',
  intro: function(s) {
    if (s.broken > 0) return '突破筑基后，你随青袍修士御剑飞行，脚下山河如棋盘。';
    return '你随青袍修士御剑飞行，脚下山河如棋盘。';
  },
  lines: [
    '云海之上，一座浮空仙山赫然入目——那便是青云剑宗。',
    '山门前，掌门负手而立："来了就好。先去藏剑阁挑柄剑。"'
  ],
  effect: { hpMax: 30, atk: 5 }
}
```

### 1.4 命格选择位置调整

**当前流程：** 开场 → 命格选择 → 灵根揭示
**新流程：** 命格选择 → 开场章节（包含灵根揭示）

**理由：**
1. 命格是"先天命运"，应该在故事开始前确定
2. 命格选择界面标题改为"天命抉择"，叙事上更合理
3. 开场章节直接使用已选的命格，无需再解释

---

## 二、完整新流程

```
1. 标题画面 → 点击"开始新人生"
2. 起名弹窗
3. Engine.startLife() 创建角色
4. ★ 命格选择（destinySelect）
   - 标题："天命抉择·你的命运"
   - 选择1-2个命格
   - 确认后显示"命运已定"
5. ★ 统一开场章节
   - 替换原 bg + ml_0_0 + ml_0_1
   - 包含：出生背景 → 道士出现 → 灵根觉醒 → 第一个选择
6. 正式进入游戏（initGame）
7. 后续主线事件按 idx 触发，每个事件前显示年度总结
```

---

## 三、代码修改清单

| 文件 | 修改内容 | 优先级 |
|------|----------|--------|
| `js/data.js` | 删除ml_0_0和ml_0_1，新增`intro`字段到所有主线事件 | P0 |
| `js/ui.js` | 修改startNewLife流程，开场章节合并 | P0 |
| `js/ui.js` | 修改endYear，添加年度总结+过渡逻辑 | P0 |
| `js/ui.js` | modifyEventChapter添加intro字段处理 | P1 |
| `js/data.js` | 新增`YEAR_SUMMARIES`文案数据 | P1 |

---

## 四、文案衔接示例

### 4.1 第一章完整流程（修改后）

```
[命格选择]
天命抉择·你的命运
命运赐予你【道体天成】和【剑意初凝】...
你选择了它们，命运的齿轮开始转动。

[开场章节]
第一章 · 凡尘旧事

你出生在一个偏僻的小村庄，父母早亡，由叔父抚养长大。
...
（完整出生叙事 + 道士出现 + 灵根觉醒 + 第一个选择）

[正式开始]
第1年 · 16岁
你每轮有3个行动点，寿元上限150岁。
```

### 4.2 后续主线触发示例

```
[第10年终]
这一年你闭关修炼，修为精进。
你已弱冠之年，修行之路才刚刚开始。

[主线触发]
第三章 · 宗门风云
你修行数年，灵气初凝，已能御风而行。
这一日，一道剑光自天际落下...
```

---

## 五、测试要点

1. **新开局测试：** 命格选择 → 开场章节 → 第一次行动，流程是否顺畅
2. **年度总结测试：** 各种行动（修炼/秘境/社交）后的总结文案是否正确
3. **主线触发测试：** 境界突破后主线事件是否正确触发
4. **存档兼容测试：** 旧存档加载后主线进度是否正确
5. **叙事连贯性测试：** 从开场到后续主线，故事是否自然衔接
