# DEDAO 法宝系统设计方案（法宝池，当前 **47** 件；本文成稿时 39 件）

> 规划日期：2026-09-08（10:03 修订版）｜ 目标版本：发布前收尾冲刺
> 依据：用户指令「灵狐设为法宝 / 剧情获取大多为法宝 / 参考异世轮回录神器（效果对标六维·战力·修炼速度·不同程度修炼·百艺·时间·锻体淬神）/ Plan 神器池」+ 08:56 数值修订与删减 + 09:17 数值修订与商店化 + 09:38 商店参数调整与新增 9 件 + **10:03 重命名 / 删减 / 条件门槛 / 新机制字段**
> 状态：**已实装（2026-09-13 装备槽制重构，39 件全生效；文档机制段已对齐，详见本报告）**
> 配套文档：**`DEDAO_法宝获取剧情_2026-09-08.md`**（每件法宝的真实获取途径 + 完整剧情/商店文案）

---

## 一、现状诊断（必须先修）

| 项目 | 现状 | 问题 |
|---|---|---|
| `s.arts` 法宝数组 | 仅 `applyOps` 的 `art:` 分支 `push` 并打日志 | **无任何派生属性读取**，等于装饰品 |
| 聚灵珠 `effect:'修炼 +5%'` | 字符串，未被 `cultGain` 消费 | 实际 0 加成 |
| 灵狐配饰 | `EQUIPS.treasure`（`dao:3`），由 `flags.pet/petGrown` 给 5%/15% 修炼 | 事件文案写"获得**法宝**：灵狐配饰"，机制却是装备+旗标，**名实不符** |
| 打神鞭 | `EQUIPS.treasure`（`atk:16`，price:0） | **无任何事件/配方产出**，玩家拿不到（孤儿项） |
| 铜钱剑 | `EQUIPS.treasure`（`atk:5`），老乞丐善心线 `equip:` 发放 | 剧情获取，应转法宝 |
| 商店系统 | `SECT_GOODS`（宗门·双货币）/ `shopStock`（游历·仅灵石）已存在 | **不发货宝**——无 `kind:'art'` 分支、无 `si.art` 处理 |

**结论**：法宝机制从零缺失。本方案先定义机制，再重分类，最后铺法宝池并把 13 件接入商店。

---

## 二、法宝机制设计（核心）

### 2.1 数据 Schema（替代原纯展示字符串）

```js
const ARTIFACTS = {
  // 六维类（结构化 effect）
  wudao_yujian: {
    name:'悟道玉简', type:'辅', grade:'天',
    desc:'论道大会上一位老修塞来的玉简，只记了一句话。',
    effect: { wu:1 }
  },
  // 战力类 + 累计类（需小钩子：战斗胜利 +1 s.killCount）
  shixue_zhu: {
    name:'嗜血珠', type:'攻', grade:'地',
    desc:'魔修祭炼的血珠，杀机越盛，锋芒越利。',
    effect: { atk:15 },
    stack: { on:'kill', stat:'atk', per:1, cap:20 }   // 每杀1敌 +1，上限 +20
  },
  // 吸血类（按造成伤害的 % 回血）
  dixue_ren: {
    name:'喋血刃', type:'攻', grade:'地',
    desc:'刃饮敌血，血归己身。',
    effect: { stealPct:0.05 }                          // 攻击造成 5% 吸血
  },
  // 修炼模式类
  xinru_zhishui: {
    name:'心如止水', type:'辅', grade:'地',
    desc:'雷雨之夜悟得的静功，心越静，进境越快。',
    effect: { modeBonus:{ normal:0.10 } }             // 普通修炼额外 +10%
  }
};
```

`effect` 支持字段：
`wu/ti/dun/shen/dao/ling`（六维，加算）｜`atk/hpMax`（战斗，加算）｜`atkPct`（攻击百分比，看 `calcAtk` 实现，小数）｜`def`（防御 flat）｜`cult`（修炼速度，如 0.05=+5%）｜`critPct/dodgePct/defPct/reflect`（**小数字段**，0.02=2%）｜`modeBonus:{normal,focus,seclusion}`（修炼倍率加算）｜`apBonus`（行动点，⚠ **通道已通但无数据源** —— `Engine.actionPoints` 已 `+artifactStats(s).apBonus`，但 47 件法宝无一提供该字段，故实际恒为 0；要启用需补一件行动点法宝，否则视作预留字段）｜`craftEff`（百艺效率倍率）｜`duantiEff`（锻体进度%）｜`duantiFree/duantiMax`（锻体免AP/上限+N）｜`cultTwice`（每年可修炼2次）｜`perYear:{wu:0.5}`（每10年结算，参 `applyDestinyYearly`）。

**09:38 / 10:03 新增字段**（均需 `artifactStats` 消费）：
`hitHeal`→已弃用（见下）｜`killHealPct`→已随狂宴匕删除｜`defToAtk`（攻击附加「防御值 × N」的伤害）｜`lowHpAtk`（气血越低攻击越高，最多 +N%）｜`tiHpBonus`（体魄提供的气血加成 +N%）｜`extraAtkChance`→已随螳螂刃删除｜`daoAtkPct`（每 1 点道心，攻击 +N%）｜`scale:{per,res:'stone',stat:'atk'|'def'|'defPct',perPoint,cap}`（灵石缩放）｜`stealPct`（攻击吸血，按造成伤害的 % 回血）。

> **单位警告**：`critPct`/`dodgePct`/`defPct`/`cult`/`craftEff`/`atkPct`/`stealPct`/`daoAtkPct` 在引擎中均为**小数**（0.05 = 5%），data 中不得写成 5 或 20。

### 2.2 聚合函数（仿 `equipStats`）

新增 `artifactStats(s)`：遍历 `s.equip.treasure`（**已装备生效槽**；库存 `s.arts` 不生效），累加全部 `effect` 字段，处理 `scale`（按 `s.stone` 等比缩放）、`stack`（按 `s.killCount` 等累加），返回与 `equipStats` 同构的对象 + 额外 `cultMul / defPct / critPct / dodgePct / atkPct / modeBonus / apBonus / craftEff / duantiEff / duantiFree / duantiMax / cultTwice / stealPct / lowHpAtk / defToAtk / tiHpBonus / daoAtkPct`。（容量见 AGENTS.md §十一：`3 + bigIdx + 道心/神识每10点各+1(≤3) + 轮回阁·先天灵宝`）

### 2.3 各系统挂钩点（最小侵入）

| 系统 | 改动位置 | 接入方式 |
|---|---|---|
| 修炼速度 | `cultGain` | `g *= 1 + artifactStats(s).cult` |
| 攻击 | `calcAtk` | `a += artifactStats(s).atk`；`atkPct` 乘算；`daoAtkPct` 按 `s.dao` 计算；`lowHpAtk` 按当前/最大气血比；`defToAtk` 加 `def*N` |
| 气血 | `calcHpMax` | `m += artifactStats(s).hpMax`；`tiHpBonus` 提升体魄→气血系数 |
| 六维 | `effAttr` / `refreshStats` | 六维值预加 `artifactStats` 六维分量 |
| 暴击/闪避/减伤 | `getCritRate` / `getDodgeRate` / `refreshStats` | `critPct+=…; dodgePct+=…; def/defPct→flatDef/earthPct` |
| 吸血 | `playerHit` | 命中结算时 `s.hp += round(dmg * stealPct)`（不超过血量上限） |
| 行动点 | `actionPoints` | `return n + artifactStats(s).apBonus` |
| 修炼模式 | `cultModes` | `m.mult += artifactStats(s).modeBonus[m.id]||0`；`cultTwice` 放开 `cultedThisYear` |
| 百艺 | `craftStudy` | `c.exp += 1 * (1+craftEff)` |
| 锻体 | `doDuanti` | 进度 `*= (1+duantiEff)`；`duantiFree` 免 `spend`；`duantiMax` 提上限 |

### 2.4 异世轮回录风格的缩放/累计/条件类

- **资源缩放**：`scale.res` 支持 `stone`（灵石），如「每 100 灵石 +1%，封顶 100%」。
- **累计类**：`stack.on:'kill'` 需在战斗胜利处 `s.killCount=(s.killCount||0)+1`（一行钩子）。
- **血量条件类**：`lowHpAtk` → 血越低攻越高，按 `(1 - hp/maxHp) * cap` 计算。
- **吸血类**：`stealPct` → 每次攻击按造成伤害的该比例回血。
- **选项条件门槛**：`choices[].cond`（`{dao:10}` / `{wu:10}` / `{shen:8}`）—— 选项仅在满足时可选/发放。需在引擎 `doEvent`/`applyChoice` 增加 `cond` 判定（复用 `evReqOK` 的字段比较逻辑）。

---

## 三、灵狐配饰 & 打神鞭 & 铜钱剑 重分类

| 物品 | 原位置 | 新位置 | 新 effect | 说明 |
|---|---|---|---|---|
| 灵狐配饰 `linghu_pei` | `EQUIPS.treasure` | `ARTIFACTS` | `{ dao:2 }` | 道心 +2；事件 `ssj_fox_accessory` 原文已称"法宝"，转为真法宝 |
| 打神鞭 `dashen_bian` | `EQUIPS.treasure` | `ARTIFACTS` | `{ shen:1 }` | 神识 +1；补来源事件 `yl_dashen` |
| 铜钱剑 `tongqian_jian` | `EQUIPS.treasure` | `ARTIFACTS` | `{ atk:5 }` | 老乞丐善心线 `ml_beggar_return` 发放（`equip:` 改 `art:`） |

> 重分类后：`EQUIPS.treasure` 仅留**可购买/可炼制**装备（古檀平安牌、镇魂墨玉、金刚降魔印、太极八卦佩、青锋剑、玄铁甲、金缕衣）；剧情获取类（灵狐配饰、铜钱剑、打神鞭、聚灵珠）均入 `ARTIFACTS`。

---

## 四、打神鞭来源事件（文案 + 数据）

新增奇遇 `yl_dashen`（无需 `req`）：

```js
{ id:'yl_dashen', cat:'jiyuan', title:'落难武修的托付', min:2, max:99,
  lines:[
    '山道旁一名浑身是血的武修靠树而坐，脚边横着一柄缠满雷纹的长鞭。',
    '"我…我撑不住了，"他攥住你的袖角，"这根打神鞭，随我斩过三山妖王……今日，便赠有缘人。"',
    '他塞来长鞭，含笑阖目。你摩挲鞭身雷纹，竟隐隐与血脉共鸣。'
  ],
  choices:[
    { t:'郑重接过，立誓不负所托', effect:{ art:'dashen_bian' },
      lines:['你对着遗体三拜："前辈放心，这鞭，我替你继续斩妖。"（获得法宝：打神鞭 · 神识+1）'] },
    { t:'只取财物，不担因果', effect:{ stone:80 },
      lines:['你取走他囊中灵石，转身离去。身后那声叹息，散在风里。'] }
  ]
}
```

---

## 五、法宝池（39 件 · 10:03 修订后）

> 品阶：黄<玄<地<天<仙。类型：攻/守/辅。
> 完整获取剧情 / 商店文案见配套文档 `DEDAO_法宝获取剧情_2026-09-08.md`。

### 5.1 剧情道具转法宝（3）
| # | 名称 | 品阶 | 类型 | 效果 | 获取 |
|---|---|---|---|---|---|
| 1 | 灵狐配饰 `linghu_pei` | 地 | 辅 | 道心 +2 | 奇遇 `ssj_fox_accessory`（选项） |
| 2 | 铜钱剑 `tongqian_jian` | 地 | 攻 | 攻击 +5 | 主线 `ml_beggar_return`（直接） |
| 3 | 打神鞭 `dashen_bian` | 地 | 攻 | 神识 +1 | 新增 `yl_dashen`（选项） |

### 5.2 A · 六维类（8）
| # | 名称 | 品阶 | 类型 | 效果 | 获取 |
|---|---|---|---|---|---|
| 4 | 太虚灵珠 `taixu_zhu` | 仙 | 辅 | 全六维 +1 | 主线 `ml_7_0`（战斗） |
| 5 | 悟道玉简 `wudao_yujian` | 天 | 辅 | **悟性 +1**（台下选项 +0.5）★ | 社交 `lundao_dahui`（选项） |
| 6 | 玄武龟甲 `xuanwu_guijia` | 地 | 守 | **防御 +20** | 秘境 `han_feng_tan`（战斗） |
| 7 | 风行羽衣 `fengxing_yuyi` | 地 | 辅 | **遁速 +2**（原+3）★ | 秘境 `xukong_lie`（探索） |
| 8 | 元神灯 `yuanshen_deng` | 天 | 辅 | 神识 +1 | 社交 `lzh_nie_xiaoqian_return`（选项） |
| 9 | 明心镜 `mingxin_jing` | 地 | 辅 | 道心 +2（原"道心镜"+3） | 奇遇 `xinyin_zhaoyu`（战斗） |
| 10 | 周天星盘 `zhoutian_xingpan` | 地 | 辅 | **悟性 +1**（坑边推演选项 神识+1，需神识≥8）★ | 秘境 `xingluo_gu`（探索） |
| — | 清净明心剑 `huixin_jian` | 天 | 辅 | **每 1 点道心，攻击 +0.5%，上限 +30%**；选项需道心≥10 | 社交 `jd_shanzhong_yinshi`（选项，见 §5.9 #36） |

### 5.3 B · 战力类（9）
| # | 名称 | 品阶 | 类型 | 效果 | 获取 |
|---|---|---|---|---|---|
| 11 | 斩仙飞刀 `zhanxian_feidao` | 天 | 攻 | **攻击 +5%**（原+30）★ | 秘境 `yuanshi_shenchu`（战斗） |
| 12 | 不灭金身 `bumie_jinshen` | 仙 | 守 | **气血 +200，防御 +5**（原受伤-8%）★ | 主线 `ml_6_0`（直接） |
| 13 | 嗜血珠 `shixue_zhu` | 地 | 攻 | 攻击 +15，**每杀1敌 +1（上限 +20）** ★ | 主线 `ml_5_0`（战斗） |
| 14 | 预知魔瞳 `yuzhi_motong` | 天 | 辅 | 暴击 +10%（**退后选项 暴击+2%**）★ | 秘境 `tianji_dao`（选项） |
| 15 | **幽魂披肩** `youhun_pijian` | 地 | 守 | **闪避 +5%**（原"幽灵披风"+12%）★ | 秘境 `baigu_gumu`（战斗） |
| 16 | 磐石铠 `panshi_kai` | 地 | 守 | 防御 +10 | 秘境 `huangshen_tan`（战斗） |
| 33 | **喋血刃** `dixue_ren` | 地 | 攻 | **攻击 5% 吸血**（`stealPct:0.05`，原"噬魂环"）★ | 秘境 `wangu_dong`（战斗） |
| 34 | 棘鳞甲 `jilin_jia` | 天 | 守 | 攻击附加「防御值 ×20%」伤害（`defToAtk:0.20`） | 秘境 `leichi` 九天雷池（战斗） |
| 35 | **血池断剑** `xuechi_duanjian` | 地 | 攻 | 气血越低攻击越高，**最多 +40%**（原"燃血匕"+60%）★ | 奇遇 `yy_moyuan_diyu`（选项） |

### 5.4 C · 修炼速度类（3）
| # | 名称 | 品阶 | 类型 | 效果 | 获取 |
|---|---|---|---|---|---|
| 17 | 聚灵珠 `juling_art` | 地 | 辅 | 修炼 +5%（既有） | 主线 `ml_guren_visit`（选项，城隍前置） |
| 18 | 道侣同心佩 `daolv_tongxin_pei` | 地 | 辅 | 修炼 +10% | 主线 `ml_3_3`（直接） |
| 19 | 长生玉髓 `changsheng_yusui` | 仙 | 辅 | **修炼 +5%**（原+20%）★ | 奇遇 `shj_xiwangmu`（选项） |

### 5.5 D · 不同程度修炼类（3）
| # | 名称 | 品阶 | 类型 | 效果 | 获取 |
|---|---|---|---|---|---|
| 20 | 静室玉牌 `jingshi_yupai` | 玄 | 辅 | 闭关额外 +10% 效果 | 主线 `ml_2_1`（直接） |
| 21 | 无尘蒲团 `wuchen_putuan` | 地 | 辅 | 闭关额外 +20% 修为（选项需悟性≥10，并体魄+1）★ | 社交 `jd_gusha_zhongsheng`（选项） |
| 22 | 心如止水 `xinru_zhishui` | 地 | 辅 | **普通修炼额外 +10%**（原+0.4倍率）★ | 奇遇 `leiyu_wudao`（选项） |

### 5.6 E · 百艺经验类（5）→ **游历商人**
| # | 名称 | 品阶 | 灵石价 | 效果 | 出现条件 |
|---|---|---|---|---|---|
| 26 | 神农锄 `shennong_chu` | 玄 | 1200 | 灵田产量 +30% | 秘境深度 ≥1 |
| 27 | 寻矿罗盘 `xunkuang_luopan` | 玄 | 1200 | 灵矿产量 +30% | 秘境深度 ≥1 |
| 29 | 丹道传承 `dandao_chuancheng` | 地 | 2600 | 炼丹研习心得 +1/次 | 大境界 ≥1 |
| 30 | 匠神锤 `jiangshan_chui` | 地 | 2600 | 炼器研习心得 +1/次 | 大境界 ≥1 |
| 31 | 百艺天书 `baiyi_tianshu` | 天 | 6000 | **全百艺效率 +20%**（原+50%）★ | 大境界 ≥2 |

### 5.7 F · 时间 / 资源缩放类（4）→ **游历商人**
| # | 名称 | 品阶 | 灵石价 | 效果 | 出现条件 |
|---|---|---|---|---|---|
| 28 | 聚宝盆 `jubao_pen` | 玄 | 1800 | 每年得灵石 = 当前 ×5% | 秘境深度 ≥1 |
| 32 | 时停月华 `shiting_yuehua` | 仙 | **6000**（原9000）★ | 每年可修炼 2 次 | 大境界 ≥3 |
| 37 | 金精匕 `jinjing_bi` | 天 | 6000 | **每 100 灵石，攻击 +1%，上限 +100%** | 大境界 ≥2 |
| 38 | 金精甲 `jinjing_jia` | 天 | 6000 | **每 100 灵石，防御 +1%，上限 +100%**（原 flat +1/150）★ | 大境界 ≥2 |

### 5.8 G · 锻体淬神效率类（4）→ **宗门功业商店**
| # | 名称 | 品阶 | 灵石/功业 | 解锁地位 | 效果 |
|---|---|---|---|---|---|
| 23 | 锻骨鞭 `duangu_bian` | 地 | 2800 / 300 | 真传 | 锻体进度 +50% |
| 24 | 淬神台 `cuishen_tai` | 天 | 8000 / 600 | 核心 | 淬神效率 +50% |
| 25 | 九转金丹炉 `jiuzhuan_jindanlu` | 仙 | 16000 / 1200 | 首席 | 锻体次数上限 +5 |
| 39 | 巨灵腰带 `juling_yaodai` | 地 | 2000 / 220 | 内门 | **体魄提供的气血加成 +25%**（原+50%）★ |

### 5.9 异世轮回录参考 · 转译件（含重命名，10:03 重排）

> 参考**效果**，命名/文案/数值全部按 DEDAO 修仙体系重做。原版效果已核实（TapTap 3.12 神器大全 / 1.16–1.17 版本公告）。

| # | DEDAO 法宝 | 参考原型 | 原版效果 | DEDAO 效果（转译） | 分类 | 获取 |
|---|---|---|---|---|---|---|
| 33 | 喋血刃 `dixue_ren` | 噬魂 | 每次攻击回复 4 生命 | **攻击 5% 吸血**（`stealPct:0.05`，原"噬魂环"） | B 战力 | 秘境 `wangu_dong`（战斗） |
| 34 | 棘鳞甲 `jilin_jia` | 尖刺铠甲 | 附加 20% 防御的伤害 | **每次攻击附加「防御值 ×20%」的伤害**（`defToAtk:0.20`） | B 战力 | 秘境 `leichi` 九天雷池（战斗） |
| 35 | 血池断剑 `xuechi_duanjian` | 绝命匕首 | 生命越低，攻击越高 | **气血越低攻击越高，最多 +40%**（`lowHpAtk:0.40`，原"燃血匕"+60%） | B 战力 | 奇遇 `yy_moyuan_diyu`（选项） |
| 36 | 清净明心剑 `huixin_jian` | 智慧之刃（智力→**道心**） | 每 1 点智力，攻击 +0.3% | **每 1 点道心，攻击 +0.5%，上限 +30%**（`daoAtkPct:0.005, cap:0.30`）；选项需 **道心≥10** | A 道心向 | 社交 `jd_shanzhong_yinshi`（选项） |
| 37 | 金精匕 `jinjing_bi` | 黄金匕首 | 每 300 金币攻击 +1%，上限 100% | **每 100 灵石，攻击 +1%，上限 +100%**（`scale`） | F 资源缩放 | 游历商人 6000 |
| 38 | 金精甲 `jinjing_jia` | 黄金甲 | 每 300 金币防御 +1%，上限 100% | **每 100 灵石，防御 +1%，上限 +100%**（`scale·defPct`） | F 资源缩放 | 游历商人 6000 |
| 39 | 巨灵腰带 `juling_yaodai` | 巨人腰带 | 体质提供的生命加成略微增加 | **体魄提供的气血加成 +25%**（每点体魄 50→62.5 气血，`tiHpBonus:0.25`） | G 锻体淬神 | 宗门功业商店 |

**数值说明**
- 喋血刃（原噬魂环）：原版"每次攻击回 4 生命"是低血量池绝对值；DEDAO 改为**按造成伤害 5% 吸血**（`stealPct`），更贴合 DEDAO 的"攻击吸血"语义，且随攻击成长。
- 金精甲：原方案为 flat 防御 +1/100 灵石（上限150），与金缕衣（百分比除数减伤）机制区分；**10:03 改为百分比 `defPct` +1%/100 灵石（上限+100%）**，与金缕衣（`jinylvDef` 除数减伤）叠加不冲突。
- 血池断剑（原燃血匕）：最多加成由 +60% 下调至 +40%。
- 清净明心剑（原慧心剑）：每点道心加成由 +2%/上限60% 下调至 **+0.5%/上限30%**，并加 **道心≥10 选项门槛**（「我修的是心」）。
- 巨灵腰带：体魄→气血加成由 +50% 下调至 **+25%**。

### 5.10 商店化配置（10:03 更新）

**宗门功业商店**（`SECT_GOODS`，`kind:'art'`，双货币）— **4 件（价格已翻倍，09:38）**

| ref | 品阶 | 灵石（翻倍） | 功业（翻倍） | 解锁地位 |
|---|---|---|---|---|
| 锻骨鞭 `duangu_bian` | 地 | 2800 | 300 | 真传 |
| 淬神台 `cuishen_tai` | 天 | 8000 | 600 | 核心 |
| 九转金丹炉 `jiuzhuan_jindanlu` | 仙 | 16000 | 1200 | 首席 |
| 巨灵腰带 `juling_yaodai` | 地 | 2000 | 220 | 内门 |

```js
const GRADE_STONE = { '黄':100, '玄':400, '地':1400, '天':4000, '仙':8000 }; // 补 '仙'
// 追加：
{ ref:'duangu_bian',        kind:'art', grade:'地', gongye:300, rankMin:'真传', stoneFix:2800 },
{ ref:'cuishen_tai',        kind:'art', grade:'天', gongye:600, rankMin:'核心', stoneFix:8000 },
{ ref:'jiuzhuan_jindanlu',  kind:'art', grade:'仙', gongye:1200, rankMin:'首席', stoneFix:16000 },
{ ref:'juling_yaodai',      kind:'art', grade:'地', gongye:220, rankMin:'内门', stoneFix:2000 }
// 注：因翻倍后不再等于 GRADE_STONE 中值，改用显式 stoneFix 字段（sectBuy 优先读 stoneFix）
```
- `sectBuy` 增分支：`else if (g.kind === 'art') { if (s.arts.indexOf(g.ref) < 0) s.arts.push(g.ref); }`
- UI `sectDoShop` 用 `ARTIFACTS[g.ref].name` 显示（现直接显示 `ref`）。

**游历商人 / 流动商贩**（`shopStock`，仅灵石）— **每次随机 3 件（09:38）**

```js
const ART_SHOP_ITEMS = [           // 池子 9 件，每次进店随机取 3 件
  { id:'shennong_chu',       price:1200, minDepth:1, minBig:0 },  // E 百艺
  { id:'xunkuang_luopan',    price:1200, minDepth:1, minBig:0 },  // E
  { id:'jubao_pen',          price:1800, minDepth:1, minBig:0 },  // F
  { id:'dandao_chuancheng',  price:2600, minDepth:0, minBig:1 },  // E
  { id:'jiangshan_chui',     price:2600, minDepth:0, minBig:1 },  // E
  { id:'baiyi_tianshu',      price:6000, minDepth:0, minBig:2 },  // E
  { id:'shiting_yuehua',     price:6000, minDepth:0, minBig:3 },  // F（10:03 由 9000 降至 6000）
  { id:'jinjing_bi',         price:6000, minDepth:0, minBig:2 },  // F
  { id:'jinjing_jia',        price:6000, minDepth:0, minBig:2 }   // F
];
```
- `shopStock` 过滤（`s.arts.indexOf(id) < 0` + 条件）后**随机取 3 件**（不足则全列）：`{ id:'ART:'+id, name:'法宝·'+ARTIFACTS[id].name, price, art:id }`
- `buyStock` 增分支：`if (si.art) out.push.apply(out, applyOps(s, { art: si.art }));`

### 5.11 修订与删减记录

**08:56 轮**：调值 6（打神鞭→神识+1、灵狐配饰→道心+2、玄武龟甲→防御+20、元神灯→神识+1、太虚灵珠→全六维+1、磐石铠→防御+10）；改名 1（道心镜→明心镜）；改效果 1（静室玉牌→闭关+10%）；**删除 10 件**（荆棘吊坠、时之沙漏、万法归元鼎、大衍神算、岁月符、灵石契约、财神金身、不灭战魂、玄牝之门、长生诀残页）。

**09:17 轮**：悟道玉简 悟性+3→+1（台下+0.5）；预知魔瞳 退后选项 悟性+2→暴击+2%；幽灵披风→幽魂披肩 闪避+12%→+5%；嗜血珠 每杀+2/封顶40→+1/封顶20；天地灵炉 删除（池33→32）；长生玉髓 修炼+20%→+5%；周天星盘 修炼+8%/悟性+1→悟性+1（C移A），坑边推演 神识+1；心如止水 +0.4倍率→额外+10%；百艺天书 +50%→+20%；E 百艺5 + F 时间资源2→游历商人；G 锻体淬神3→宗门功业商店。

**09:38 轮**：游历商人随机 3 件（原1~2）；宗门原3件价格翻倍（灵石/功业×2，改 `stoneFix`）；零门槛善心选项不补；新增 9 件（噬魂环/狂宴匕/棘鳞甲/燃血匕/金精匕/金精甲/慧心剑/螳螂刃/巨灵腰带）；宗门商店新增第4件「巨灵腰带」；游历池 7→9。

**10:03 轮（本轮）**：
| 项 | 改动 |
|---|---|
| 燃血匕→**血池断剑** | 重命名；最多加成 +60% → **+40%**（`lowHpAtk:0.40`） |
| 噬魂环→**喋血刃** | 重命名（`dixue_ren`）；效果 命中回6血 → **攻击 5% 吸血**（`stealPct:0.05`）；新增 `stealPct` 机制字段 |
| **删除 螳螂刃** | 池 41→40；移除 `extraAtkChance` 钩子需求 |
| 巨灵腰带 | 体魄→气血 +50% → **+25%**（`tiHpBonus:0.25`） |
| 金精甲 `jinjing_jia` | 每100灵石 防御+1(封顶150) → **防御+1%(封顶+100%)**（`scale·defPct`） |
| 时停月华 | 售价 9000 → **6000** |
| 斩仙飞刀 | 攻击 +30 → **攻击 +5%**（`atkPct:0.05`） |
| 不灭金身 | 气血+200/受伤-8% → **气血+200/防御+5** |
| **删除 狂宴匕** | 池 40→39；移除 `killHealPct` 钩子需求 |
| 清净明心剑 `huixin_jian` | 原"慧心剑"；每点道心攻击+2%/上限60% → **+0.5%/上限30%**；选项「我修的是心」加 **道心≥10** 门槛 |
| 无尘蒲团 | 「坐的是一个空」选项加 **悟性≥10** 门槛，并 **体魄+1** |
| 心如止水 | 淬体选项 体魄 +2 → **+3** |
| 周天星盘 | 「下坑取出星盘」选项加 **神识≥8** 门槛 |
| 风行羽衣 | 遁速 +3 → **+2** |

> **分布（10:03）**：转化3 / A8 / B9 / C3 / D3 / E5 / F4 / G4 = **39 件**。
> **获取渠道**：剧情 **26 件**（主线7 / 奇遇7 / 社交5 / 秘境7）、商店 **13 件**（宗门4 / 游历9）。
> **游历商人池 9 件，每次随机 3 件**。

---

## 六、实施步骤清单（确认后执行）

1. **机制层**：`engine.js` 新增 `artifactStats(s)`；接入点——
   - 属性：`refreshStats` / `cultGain` / `calcAtk`（`atk`、`atkPct`、`daoAtkPct`、`lowHpAtk`、`defToAtk`、`scale.atk`）/ `calcHpMax`（`hpMax`、`tiHpBonus`）/ `getCritRate` / `getDodgeRate` / `getExtraAtkChance` 已不再需要（螳螂刃已删）
   - 系统：`actionPoints` / `cultModes` / `craftStudy` / `doDuanti`
   - 战斗：`playerHit` 加 `stealPct`（命中按伤害 % 回血）
2. **战斗钩子**：`playerHit` 内 `b.done && b.win` 处加 `s.killCount++`（支持 `stack.on:'kill'`，嗜血珠）。
3. **选项条件门槛**：`doEvent`/`applyChoice` 增 `cond` 判定（复用 `evReqOK` 字段比较）——用于 清净明心剑（道心≥10）、无尘蒲团（悟性≥10）、周天星盘（神识≥8）。
4. **数据层**：`data.js` 重写 `ARTIFACTS` 为结构化 effect（**39 件**）；`EQUIPS.treasure` 移除 `linghu_pei`/`dashen_bian`/`tongqian_jian`；`GRADE_STONE` 补 `'仙':8000`。
5. **商店层**：`SECT_GOODS` 追加 4 件 `kind:'art'`（含 `stoneFix` 字段，`sectBuy` 优先读 `stoneFix`）；新增 `ART_SHOP_ITEMS` **9 件**（含降价后的 `shiting_yuehua`）；`sectBuy` / `buyStock` / `shopStock`（**随机 3 件**）增法宝分支；`sectDoShop` UI 显示法宝名。
6. **事件修正**：灵狐事件 → `art:'linghu_pei'`；`ml_beggar_return` `equip:` → `art:`；新增 `yl_dashen`；其余 23 件剧情法宝按配套文档逐条挂接（含 `wangu_dong`/`leichi`/`yy_moyuan_diyu`/`jd_shanzhong_yinshi` 等；`jilin_jia` 挂 `leichi`）；无尘蒲团/周天星盘/心如止水按本轮门槛改 `choices`，清净明心剑改写 山中隐士 事件文案与门槛。
7. **UI 层**：法宝背包展示 effect；炼制界面排除法宝（已完成）；确认 `formulaItem` 不误判法宝。
8. **测试**：扩展 `test/automated` 校验「法宝 effect 真实生效（含 stealPct/lowHpAtk/defToAtk/daoAtkPct/scale/tiHpBonus）」「39 件 id 唯一」「字段合法（百分比为小数）」「配方不得含法宝」「商店商品 ref 均存在于 ARTIFACTS」「选项 cond 门槛正确拦截」。
9. **构建**：`bash tools/build_release.sh`。

---

## 七、待确认事项（10:03 更新）

1. **「下文的法宝机制」已对应**：即本方案 §2（含 `scale` 资源缩放 / `stack` 累计 / `lowHpAtk` 血量条件 / `stealPct` 吸血 / `cond` 选项门槛 —— 对标异世轮回录神器）。
2. **铜钱剑已转法宝**：与灵狐配饰、打神鞭一并归入 `ARTIFACTS`。
3. **零门槛善心选项：已决定不补**（09:38）。保留 `ml_0_5` 的 `req:{stone:50}` 门槛。
4. **商店参数已定**：游历商人每次**随机 3 件**（池 9 件）；宗门功业商店 **4 件**（含巨灵腰带，价格翻倍，用 `stoneFix`）。
5. **宗门商店保持 4 件**（10:03 未要求移除巨灵腰带），其数值已下调至 +25%。
6. **是否现在实装**：本文件为规划稿。确认后按 §六 执行，并按配套文档写入全部获取剧情与商店文案。

---

*数据依据：解析 `js/data.js`（`ARTIFACTS`/`EQUIPS`/`MAINLINE` 34 章/`EVENTS`/`SECT_GOODS`/`GRADE_STONE`/`SHOP_ITEMS`/`NPCS`）与 `js/engine.js`（`equipStats`/`refreshStats`/`cultGain`/`calcAtk`/`calcHpMax`/`getCritRate`/`getDodgeRate`/`enemyAtkRoll`/`doDuanti`/`actionPoints`/`shopStock`/`buyStock`/`sectGoods`/`sectBuy`/`applyOps`）。*
