# DEDAO 轮回结算收益 + 成就体系 重做方案

> 状态：**已实装**（`earnPoints` / `settlePoints` / `computeAdvPoints` 均在 `js/engine.js`；成就不翻倍、K=0.05、删 `jie×2` 已落地）。标题原带「（Plan）」为成稿期遗留，2026-09-15 去除。
> 数字对账：本方案的 §7.1 / §7.2 / §7.3 / §九 四张表由 `node tools/reinc_validate.js`（测试7）**自动对账** —— 脚本读本文档断言，改动其中任一数字而不重跑脚本会立即报红。
>
> 目标：重做「轮回结算收益」与「成就体系」。
> 原则：**保留境界/渡劫/死劫三大进度类获益；参考异世轮回录的轮回收益体系；砍掉战斗收益；新增并平衡秘境探索收益；重做成就体系去战斗向。**
> 用户拍板结论（已全部通过）：死劫获益显式化、秘境单段分与境界档对齐、轮回点永久+属性上限做(二期)、**成就不翻倍（删除翻倍说法）**、**高劫最终加成 K=0.05（比例形式）+ 删除原 `jie×2` 平加**。

---

## 一、参考依据：异世轮回录的轮回收益体系

| 异世轮回录要点 | 对 DEDAO 的启发 |
|---|---|
| 每世结束按「**成就 + 总属性数值 + 声望/进度**」发放魂能/轮回点 | 轮回点 = 成就 + 进度 + 探索广度（**非击杀**） |
| 魂能用于**永久提升基础属性、解锁初始天赋/出身**，多周目叠加 | 沿用「轮回天赋树」永久加成；二期扩展"直接永久+属性上限" |
| 明确**不按击杀/战斗场次**发点 | **砍掉一切战斗收益**；死劫/渡劫/秘境之主虽含战斗，但点源基于"通过/探索进度"而非"杀怪数" → 属进度类，保留 |
| 多结局/多路线收集 | 成就奖励"人生多样性/探索/结局收集/悟道" |

**当前 DEDAO 缺口**：`earnPoints` 里**完全没有秘境探索收益项**（`advClearReward` 只给局内灵石/装备）；成就里 `mo_yuan`(镇魔渊) 是纯战斗结局成就 → 这两处正是重做重点。

---

## 二、新结算公式（五类求和 + 劫数倍率 + 高劫最终加成）

```
本世轮回点 = round( ( 境界档 + 渡劫分 + 死劫分 + 秘境探索分 + 新成就分 )
                    × endMul × (1 + jie×0.2) )
           + round( base × jie × K )    // 【新增·收敛】高劫数最终加成（比例形式，K=0.05 已拍板）
```
- `jie` = 当前叠劫层数（0–9）。
- **`endMul` 结局乘子**（2026-09-15 补记）：打破轮回 / 隐藏胜 **1.5**；飞升 / `idx≥15` / 境界为仙 **1.2**；其余 **1.0**（engine.js:4740）。原式漏写，导致按本式手算的飞升场景偏低约 27%。
- **高劫最终加成（最终版）**：采用**比例形式** `round(base×jie×K)`（**K=0.05，用户拍板**）。原 `jie×2` 平加已**删除**（避免低阶高劫病态膨胀）。
- 演进：最初提案 flat `jie×5`（炼气初陨 6→80，约13倍膨胀，否决）→ 比例 K=0.1（最坏 6.67x）→ 收敛至 **K=0.05 + 删 jie×2**（最坏 3.33x，角落炼气/jie9=20，温和）。真实游戏里 jie 与境界正相关、低阶高劫几乎不出现。

| 类别 | 取值（重做后最终值） | 性质 | 处理 |
|---|---|---|---|
| **境界档**（保留） | 炼气2 / 筑基4 / 金丹7 / 元婴11 / 仙25 | 进度 | 保留原值 |
| **渡劫分**（保留） | `min(10, 渡劫次数 × 3)` | 进度 | **2026-09-15 更正**：渡劫次数 = `s.tribPassed`（金丹劫/元婴劫/飞升劫，至多 3 次）；原文档写的 `⌊渡劫次数/3⌋` 为旧稿，且不得误用 `s.broken`（那是突破次数） |
| **死劫分**（保留·显式化） | 当世**通过死劫数 × 2** | 进度 | 原仅靠 jie 倍率隐式，现显式成项 |
| **死劫分**（保留·显式化） | 当世**通过死劫数 × 2** | 进度 | 原仅靠 jie 倍率隐式，现显式成项 |
| **秘境探索分**（新增） | 见第三节 | 探索 | **新增** |
| **新成就分**（重做·不翻倍） | 见第四节 | 成就 | 去战斗向，按原点数 |

---

## 三、秘境探索收益平衡（重点）

**核心：奖励"探索度/通关/广度"，绝不奖励击杀场次。**

| 子项 | 规则 | 点数 |
|---|---|---|
| 单秘境首通 | 探索度满 100% **且** 击败秘境之主（按 `s.flags.advClear[key]` 去重） | 黄2 / 玄4 / 地7 / 天11 / 仙15（**与境界档对齐**） |
| 探索行为奖 | 探索度满 100% 但中途撤退保收获 | +1 / 段 |
| 广度/全收集 | 当世通关品级数 ≥2→+2；≥3→+4；≥4→+8 | +2/+4/+8 |
| 单世上限 | 秘境探索分 ≤ 当前境界档 × 2 | 封顶 |

---

## 四、成就体系重做（去战斗向，不翻倍）

> **用户决定：成就奖励不翻倍**，下表为最终落地值。

| 操作 | 成就 | 点数 | 说明 |
|---|---|---|---|
| **保留** | 破境·筑基 | +2 | 进度 |
| 保留 | 金丹大道 | +3 | 进度 |
| 保留 | 元婴出窍 | +4 | 进度 |
| 保留 | 羽化登仙（飞升） | +10 | 结局（非战斗） |
| 保留 | 道侣同心 | +2 | 人生关系 |
| 保留 | 寿终正寝 | +1 | 人生 |
| 保留 | 三次渡劫 | +3 | 进度 |
| 保留 | 双甲子（200岁） | +2 | 人生 |
| **砍掉** | 镇魔渊（mo_yuan +8） | — | **纯战斗结局，移除** |
| **新增** | 初探秘境 | +1 | 探索 |
| 新增 | 秘境收藏家 | +6 | 探索广度 |
| 新增 | 遗世寻仙 | +4 | 探索 |
| 新增 | 悟道 | +5 | 结局（非战斗） |
| 新增 | 百家之长 | +3 | 人生多样性 |
| 新增 | 长生久视 | +3 | 人生 |
| 新增 | 散修成道 | +4 | 人生多样性 |

---

## 五、明确砍掉的「战斗收益」

- ❌ 任何"击杀数 / 战斗场次 / Boss 击杀"相关的轮回点来源。
- ❌ `mo_yuan`(镇魔渊) 战斗结局成就。
- ❌ 战力/攻击数值挂钩的点源。
- ✅ 保留：死劫/渡劫/秘境之主"通过/探索进度"类收益（属进度，非战斗 grind）。

---

## 六、轮回点用途（真实系统澄清）

> **澄清**：DEDAO 有**两套**与"轮回点"相关的系统，此前混淆，现在厘清：
>
> **A. 轮回天赋树 `REINCARNATION`（data.js:2453）—— 即用户所说的"轮回阁"**
> - 包含六维（慧根/强体/灵步/神念/定心/灵海）+ 其余天赋（殷实/道种/延寿/命格栏等）。
> - 消耗池 = **`meta.points`**（跨周目累积，ui.js:5073 `M.points -= currentCost`，**已正确实现**）。
> - 升级成本公式（ui.js:5058）：第 n 级花费 = `r.cost × n`，即单天赋满级 = `r.cost × max(max+1)/2`。
>
> **B. 开荒池加成 `REINC_TALENT`（data.js:2984）—— 次要小系统**
> - 每级给每世开荒池 +bonus，升级函数 `reincTalentUpgrade` 扣 `s.reincPoints`（**该字段从未被赋值，存在 bug**，第十节步骤处理）。
> - 此系统与"轮回阁天赋树"是两回事，加满仅 150 点，并非用户所指的轮回阁。

- 二期扩展：允许用轮回点**直接永久 +某基础属性上限**（呼应异世轮回录），多周目叠加滚雪球。

---

## 七、轮回天赋树加满总点数与游戏局数衡量

### 7.1 加满总点数（真实 `REINCARNATION` 成本公式：第 n 级 = cost×n；六维 max=9）

| 天赋 | cost | max | 满级成本 |
|---|---|---|---|
| 慧根(悟性) | 6 | 9 | **270** |
| 定心(道心) | 6 | 9 | **270** |
| 强体(体魄) | 3 | 9 | 135 |
| 灵步(遁速) | 3 | 9 | 135 |
| 神念(神识) | 3 | 9 | 135 |
| 灵海(灵力) | 3 | 9 | 135 |
| 道种(修炼) | 6 | 5 | 90 |
| 殷实/见面礼/丹心/器魂/延寿/小绿瓶/灵田 | 2~3 | 3~4 | 12~20 各 |
| 舍生 | 5 | 3 | 30 |
| 大千命格 | 3 | 4 | 30 |
| 我命由我/天命锁定 | 12 | 1 | 12 各 |

- **六维核心全满 = 270+135×4+270 = 1080 点**（验证：道心270+悟性270=540 ✓；单维每级+1属性，满9级共+9）
- **全部天赋拉满 = 1296 点**（六维1080 + 其余216；由 `reinc_validate.js` 直读 data.js 实算。**2026-09-15 更正**：原写 1406 是脚本里的过期硬编码断言，实测为 1296）

### 7.2 各场景单局收益（重做落地版·含秘境探索+新成就·K=0.05·删 jie×2）
| 场景（本世终结） | jie0 | jie3 | jie6 | jie9 |
|---|---|---|---|---|
| 炼气初陨 | 6 | 11 | 15 | 20 |
| 筑基陨 | 18 | 32 | 45 | 58 |
| 金丹陨 | 37 | 65 | 92 | 121 |
| 元婴陨 | 61 | 107 | 152 | 198 |
| 飞升 | 164 | 284 | 403 | 522 |

> 由 `reinc_validate.js` 直读 data.js 实算（K=0.05、删 jie×2、含 endMul）。
> **2026-09-15 更正**：旧表 6/18/34/55/129 是脚本用错字段（`s.broken` 当渡劫次数、漏 `endMul`）算出的**错误镜像产物**，已随脚本一并修正。
> 最坏倍数 = 炼气/jie9÷jie0 = 20÷6 ≈ **3.33x**（各场景均 ≤ 3.33x），远优于旧 flat（80，13x）。各场景 base 构成见 §九。

### 7.3 加满所需局数（每局稳定达成该场景，忽略 reroll 消耗）
| 场景 | 六维全满(1080) jie0 | 六维全满 jie9 | 全天赋全满(1296) jie0 | 全天赋全满 jie9 |
|---|---|---|---|---|
| 炼气初陨 | 180 | 54 | 216 | 65 |
| 筑基陨 | 60 | 19 | 72 | 23 |
| 金丹陨 | 30 | 9 | 36 | 11 |
| 元婴陨 | 18 | 6 | 22 | 7 |
| 飞升 | 7 | 3 | 8 | 3 |

### 7.4 现实 progression 区间结论
- **六维全满(1080点)**：长期停在炼气/筑基（jie0）需 60~180 局；中性玩法（金丹~元婴，jie0）**18~30 局**；高效（飞升，jie0）**7 局**、jie9 **3 局**。
- **全天赋全满(1296点)**：同上口径 → 72~216 局 / **22~36 局** / 8 局（jie9 3 局）。
- 注：六维 max 由 5→9 后加满成本约 3 倍（360→1080），故局数相应拉长；若觉过长，可下调六维 cost 或维持 max 但降单级 cost。
- 若玩家每局都花点 reroll 命格（每次 1 点），加满局数相应延长；上述为纯攒的下限。

---

## 八、关键决策（已全部拍板通过）

| # | 问题 | 决策 | 状态 |
|---|---|---|---|
| 1 | 死劫获益显式化 | **显式化**（当世通过死劫×2） | ✅ |
| 2 | 秘境单段分与境界档对齐 | **对齐** | ✅ |
| 3 | 轮回点永久+属性上限 | **做**（二期） | ✅ |
| 4 | 成就 pts | **不翻倍**（恢复原点数） | ✅ |
| 5 | 高劫数额外轮回点加成 | **加入，比例形式 `round(base×jie×K)`（K=0.05 已拍板），并删除原 `jie×2` 平加 | ✅ |

---

## 九、平衡校验预期（不翻倍版，实跑 `reinc_sim.js` 验证）

| 场景 | 境界档 | 渡劫分 | 死劫分 | 秘境分(≤realm×2) | 成就分 | 预估本世(jie0) |
|---|---|---|---|---|---|---|
| 炼气初陨 | 2 | 0 | 0 | 2（黄首通） | 2 | 6 |
| 筑基陨 | 4 | 0 | 2（1死劫） | 8（黄+玄+广度2，封顶8） | 4 | 18 |
| 金丹陨 | 7 | 3（1渡劫） | 4（2死劫） | 14（黄+玄+地+广度4，封顶14） | 9 | 37 |
| 元婴陨 | 11 | 6（2渡劫） | 8（4死劫） | 22（四常规+广度8，封顶22） | 14 | 61 |
| 飞升 | 25 | 9（3渡劫） | 8（4死劫） | 47（五秘境+广度8，封顶50） | 48 | 164 |

> 飞升行的合计 base = 25+9+8+47+48 = **137**，再乘 `endMul=1.2`（飞升）→ round(164.4) = **164**；其余四行 `endMul=1.0`，base 即 jie0。
>
> 校验结论（2026-09-15 重跑 `reinc_validate.js`）：jie0 列 = **6 / 18 / 37 / 61 / 164**，与该表一致（脚本已加「读本文档断言」的测试7，改这里不跑脚本就会红）。各场景「成就分 ≈ 0.4~1.9 × 境界档」，飞升集大成属合理；秘境分受 `≤realm×2` 封顶，低境界越级刷被有效压制。**高劫加成（K=0.05）最坏倍数 3.33x（炼气/jie9），收敛**。
>
> ⚠ 旧结论写的「jie0 = 6/18/34/55/129」为**错误镜像**：`reinc_validate.js` 当时把 `s.broken`（突破次数）当渡劫次数、且漏了 `endMul`，故 金丹/元婴/飞升 三行整列偏低。已修脚本 + 修本文档。

---

## 十、实施步骤（拍板后执行顺序）

1. **`data.js` - 成就表**：`ACHIEVEMENTS` 删除 `mo_yuan`；其余保留项保持原 pts（2/3/4/10/2/1/3/2）；新增 7 项（初探1/收藏家6/遗世4/悟道5/百家3/长生3/散修4）。
2. **`data.js` - 秘境点配置**：新增 `ADV_POINTS`（品级分值 + 广度阈值 + 单世上限）；`s.flags.advClear[key]` 作首通去重。
3. **`engine.js` - `earnPoints` 重写**：
   ```
   realm = REALM_TIER[s.realm]
   trib  = min(10, floor(s.broken/3))
   death = s.deathPassed * 2            // 死劫胜利时 +1 计数（当世，ui.js 死劫胜利分支累加）
   adv   = computeAdvPoints(s)          // 封顶 realm*2
   ach   = sumUnlockedThisLife(s)       // 当世新解锁成就分（不翻倍）
   base  = realm + trib + death + adv + ach
   return round(base*(1+jie*0.2)) + round(base*jie*K)   // 高劫最终加成（比例 K=0.05，已删原 jie×2 平加）
   ```
4. **`engine.js` - `settlePoints`**：breakdown 扩为 `{ realm, trib, death, explore, ach }`。
5. **【小 bug 修复】开荒池系统**：`reincTalentUpgrade` 扣 `s.reincPoints`（从未赋值）→ 统一改为扣 `meta.points`（与 `REINCARNATION` 同池）；或直接废弃该小系统、并入轮回天赋树。轮回天赋树 `REINCARNATION` 本身已正确扣 `meta.points`，无需改。
6. **`ui.js` - `renderSettlement`**：明细面板扩 5 项；删除旧"长寿+2/特殊壮举+10"装饰行（不计入 total，易误导）。
7. **`tools/reinc_sim.js`**：已用真实成本公式与收敛高劫加成建模，实跑第七/九节校验。
8. **二期（决策3）**：轮回天赋树新增"永久+属性上限"消耗项，UI 接入。
9. **回归**：75 项测试 + 真实多周目手感。

---

## 十一、风险与回退

- **成就不翻倍后点源偏少**：若校验发现非飞升场景成就分过低、加满局数 > 上限，回退为"保留项翻倍、新增项原值"或整体 ×1.3。需二次确认。
- **高劫加成膨胀**：最终采用比例 `round(base×jie×K)`（**K=0.05**）并删除原 `jie×2` 平加；最坏倍数 3.33x（角落炼气/jie9=20），已收敛。若后期仍觉膨胀，可对加成封顶 `min(base×jie×K, base×1.0)`。
- **秘境封顶失效**：`advClear` 去重键若与存档结构不符会重复计分；实现时先打印 breakdown 核对。
- **死劫计数**：`s.deathPassed` 需在死劫胜利分支 +1，且跨世清零（当世计数）。

确认后我即按第十节步骤落地代码并跑平衡模拟。
