# DEDAO（得道）· 修仙放置文字游戏 — 项目简介

## 一、项目简介

DEDAO 是一款以东方修仙为背景的**放置 / 养成类浏览器游戏**。玩家扮演一名修士，从炼气起步，历经历练、战斗、奇遇、宗门、轮回，一路向长生大道修行。游戏采用原生 JavaScript + HTML + CSS 构建，配合 PWA 支持离线运行，规划上线抖音小游戏、TAPTAP、华为小游戏三大平台（统一 WebView 嵌入），定位为纯免费、无内购的休闲修仙体验。

- **技术栈**：原生 JS / HTML / CSS + PWA，零第三方框架依赖
- **架构**：`data.js`（配置与剧情）/ `engine.js`（状态与核心逻辑）/ `ui.js`（渲染与交互）三层模块化
- **形态**：浏览器 H5，桌面与移动端自适应，可安装为 PWA 应用
- **内容规模**：多章主线、99 个随机事件（游历奇遇 68 / 秘境 14 / 山河 13 / 年末 4）、47 件可收集法宝、46 个命格、93 部功法、14 项轮回天赋、22 条炼制配方、5 处秘境、66 个成就

## 二、职责（个人独立开发）

| 维度 | 具体内容 |
| --- | --- |
| 架构与选型 | 独立完成技术选型与原生零依赖架构设计，确立 data / engine / ui 三层拆分 |
| 核心系统开发 | 放置挂机、修炼境界、战斗（属性 / 暴击 / 闪避 / 吸血）、装备、法宝、秘境、宗门功业、轮回天赋等 |
| 剧情与内容 | 编写主线章节与游历奇遇剧情，设计 47 件法宝的数值模型与获取路径 |
| 质量保障 | 搭建自动化测试（**298 项用例**），覆盖数据完整性、机制生效、UI 渲染（jsdom 真实 DOM）、跨版本存档兼容 |
| 多端适配 | 适配移动端 WebView，处理 confirm 阻塞、本地存储、PWA 离线等问题 |
| 发布交付 | 编写发布打包脚本，剔除开发与源文件，产出可直接部署的发布包 |

## 三、关键业绩

| 业绩 | 说明 |
| --- | --- |
| 0→1 完整可玩 | 从零独立交付一款结构完整、内容可玩、可发布的修仙放置游戏 |
| 法宝系统全链路落地 | 设计 47 件法宝结构化数值模型，打通「剧情发放 → 属性聚合 → 界面展示 → 商店购买 → 战斗掉落」全链路 |
| 自动化测试 298/298 通过 | 关键机制与挂接均有回归测试守护，含法宝属性真实生效校验、UI 端到端渲染（jsdom）与变异验证 |
| 包体精简、零依赖 | 原生实现无框架，发布包体轻量，适合 WebView 嵌入与静态托管 |
| 三大平台发布路径打通 | 抖音 / TAPTAP / 华为小游戏统一 WebView 方案，纯免费无内购 |

---

> **数量口径来源**（2026-09-15 二次校核）：法宝 / 命格 / 功法 / 轮回天赋 / 配方 / 事件 / 秘境 / 成就
> 均取自代码中的**唯一真源** `js/data.js`（`ARTIFACTS` 47 / `DESTINIES` 46 / `TECHNIQUES` 93 /
> `REINCARNATION` 14 / `FORMULAS` 22 / `EVENTS` 99 / `ADVENTURE_CONFIG` 5 / `ACHIEVEMENTS` 66）；
> 测试用例数取自 `test/automated/` 全量运行结果。核对命令：`node test/automated/run.js`（当前 **298/298**）。
>
> ⚠ **易错点**：`js/data.js` 里 `TALENTS`（28 条，凡命/本命/奇命/极命/仙命）是**旧「开局命格」表**，
> 引擎注释即写「旧命格（TALENTS）」；现行命格是 `DESTINIES`（46）。**不要把 28 当命格或天赋数** ——
> 本文档曾误写为「28 个轮回天赋」，实为把旧表条数错挂到 `REINCARNATION`（14）上。
>
> **数据增删或测试用例数变化后，请同步刷新本文档的数字** —— 同类漂移已在成就文案（#56）、
> AGENTS.md 测试模块表（#57）上各发生过一次。
