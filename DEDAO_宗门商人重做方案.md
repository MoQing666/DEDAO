# DEDAO · 宗门商人重做方案（定稿）

> 状态：**已实装**（2026-09-08；测试数见当前全量运行结果 —— 原写「76/76」为当时快照）。
> 依据：用户已确认 4 项决策 —— ① 保留通用功法（不做本门专属）② 增加消耗品循环（丹药/灵材）③ 不取消法宝翻倍定价 ④ 先定稿再动代码。
> **定价语义（设计意图）**：用户明确「翻倍指的是**功业标价**（gongye），不影响**灵石标价**（stone）」。故法宝灵石 = `GRADE_STONE[grade]` 中值（**不**翻倍），法宝功业 = 2× 同档功法基础价（**翻倍**）。原文档「法宝 stoneFix≈2× 中值」表述已废弃。
>
> ⚠ **2026-09-15 核对发现的代码↔文档分歧（未决，勿按任一单方动手）**：4 件宗门法宝（巨灵腰带/锻骨池/淬神台/九转金丹炉）在代码里**均改用 `coin:'stone'` + `stoneFix` 锁定灵石价、且完全没有 `gongye` 字段** —— 即「法宝功业翻倍」这一决策**在代码中未落地**，玩家实付**纯灵石**，功业列不存在。同时 4 件的 `grade` 已在 2026-09-13 重平衡时下调（如巨灵腰带 地→玄、九转金丹炉 仙→天），故 §二 表里的品级/价格与代码现值不一致。
> **口径原则**：要落地「功业翻倍」就**改代码**（给 4 件加 `gongye` 并补 `sectBuy` 的双货币分支），要放弃就**改本表**；两者选一后再同步文档，不要只改文档把它写回旧数值（否则下次核对又报分歧）。见 `DEDAO_三方案推进对齐_2026-09-13.md` §3.2/§3.4。

---

## 一、现状诊断（问题 → 本方案对策）

| 问题 | 现象 | 本方案对策 |
|---|---|---|
| 品类单一 | 当前 18 件里 12 件是功法，无装备/丹药/灵材 | 扩到 **7 类**：功法 / 遁术 / 阵法 / 丹药 / 灵材 / 装备 / 法宝 |
| 低阶没得买 | 外门仅生息功/青锋剑(无)/聚灵阵 | 外门即开放 **丹药+灵材+装备+黄级功法** 稳定消费 |
| 功法撞秘境池 | 纯阳/天罡/庚金/麒麟/白虎 与秘境掉落重叠 | **保留**（用户决策①）——宗门=通用功法铺，秘境=奇遇专属，定位不同 |
| 价格割裂 | 功法用 `GRADE_STONE` 中值，法宝用 `stoneFix` 翻倍 | **维持双轨**（用户决策③）——法宝 `stoneFix≈2×` 同级中值，不取消翻倍 |
| 死字段 | `SECT_RANKS.discount` / `goods` 引擎从未使用 | 见 §五 可选增强，默认不在本次范围 |

---

## 二、新 SECT_GOODS 全表（按地位分组）

> 字段说明：`ref` 取自既有数据表（TECHNIQUES / ELIXIRS / MATERIALS / EQUIPS / ARTIFACTS）；`灵石价`：功法/遁术/阵法=GRADE_STONE 中值，法宝=stoneFix（翻倍），丹药/灵材=显式 stoneFix；`qty`=灵材/丹药单次购入数量。

### 外门（rankMin: 外门）
| 商品 | 类别 | 品级 | 灵石价 | 功业价 | 效果/说明 |
|---|---|---|---|---|---|
| 生息功 | tech | 黄 | 100 | 20 | 修炼倍率 ×1.15（保留） |
| 聚灵阵 | array | - | 0 | 40 | 年灵石产出提升（保留） |
| 聚气丹 | elixir | - | 60 | 10 | 修炼时自动服用，本次收益加倍 |
| 黄级灵草 ×10 | mat | 黄 | 40 | 5 | 炼丹材料（bundle） |
| 黄级灵铁 ×10 | mat | 黄 | 40 | 5 | 炼器材料（bundle） |
| 青锋剑 | equip | 黄 | 100 | 15 | 攻击 +20 |

### 内门（rankMin: 内门）
| 商品 | 类别 | 品级 | 灵石价 | 功业价 | 效果/说明 |
|---|---|---|---|---|---|
| 纯阳功 | tech | 玄 | 400 | 60 | 修炼倍率 ×1.50（保留） |
| 天罡诀 | tech | 玄 | 400 | 60 | 修炼倍率 ×1.50（保留） |
| 筑基丹 | elixir | - | 200 | 40 | 突破筑基成功率 +25% |
| 玄级灵草 ×10 | mat | 玄 | 150 | 20 | 炼丹材料（bundle） |
| 玄级灵铁 ×10 | mat | 玄 | 150 | 20 | 炼器材料（bundle） |
| 玄铁甲 | equip | 玄 | 300 | 50 | 气血上限 +150 |
| 巨灵腰带 `juling_yaodai` | art | 地 | 1400（中值·不翻倍） | 300（2×150 翻倍；**代码当前 `coin:'stone'` 仅灵石，功业翻倍语义待拍板**） | 锻体淬神类法宝（体魄→气血 +25%） |

### 真传（rankMin: 真传）
| 商品 | 类别 | 品级 | 灵石价 | 功业价 | 效果/说明 |
|---|---|---|---|---|---|
| 庚金诀 | tech | 地 | 1400 | 150 | 修炼倍率 ×1.80（保留） |
| 丙火诀 | tech | 地 | 1400 | 150 | 修炼倍率 ×1.80（保留） |
| 影遁术 | dun | 玄 | 400 | 80 | 逃生 60% / 减伤 10%（保留） |
| 缩地成寸 | dun | 地 | 1400 | 200 | 逃生 100% / 减伤 20%（保留） |
| 五行阵 | array | - | 0 | 100 | 五行阵法（保留） |
| 结金丹 | elixir | - | 800 | 120 | 突破金丹渡劫 +25% |
| 地级灵草 ×10 | mat | 地 | 500 | 80 | 炼丹材料（bundle） |
| 地级灵铁 ×10 | mat | 地 | 500 | 80 | 炼器材料（bundle） |
| 锻骨鞭 | art | 地 | 1400（中值·不翻倍） | 300（2×150 翻倍） | 锻体淬神类法宝（保留） |

### 核心（rankMin: 核心）
| 商品 | 类别 | 品级 | 灵石价 | 功业价 | 效果/说明 |
|---|---|---|---|---|---|
| 麒麟诀 | tech | 天 | 4000 | 300 | 修炼倍率 ×2.30（保留） |
| 白虎诀 | tech | 天 | 4000 | 300 | 修炼倍率 ×2.30（保留） |
| 元婴丹 | elixir | - | 2000 | 250 | 突破元婴渡劫 +25% |
| 天级灵草 ×10 | mat | 天 | 1200 | 150 | 炼丹材料（bundle） |
| 天级灵铁 ×10 | mat | 天 | 1200 | 150 | 炼器材料（bundle） |
| 淬神台 | art | 天 | 4000（中值·不翻倍） | 600（2×300 翻倍） | 锻体淬神类法宝（保留） |

### 首席（rankMin: 首席）
| 商品 | 类别 | 品级 | 灵石价 | 功业价 | 效果/说明 |
|---|---|---|---|---|---|
| 悟道丹 | elixir | - | 4000 | 400 | 服用后道心 +1 |
| 九转金丹炉 | art | 仙 | 8000（中值·不翻倍） | 1200（2×600 翻倍） | 仙级法宝（保留） |

> 合计 **29 件**，覆盖 7 类（`SECT_GOODS` 实测 29 件，原写 30 为笔误；2026-09-15 核对更正）。低阶（外门/内门）以丹药+灵材+装备构成稳定消耗闭环，高阶（真传+）以功法+法宝构成追求目标。

---

## 三、引擎改动清单（实装时执行）

### 1. `sectBuy`（engine.js ~3076）扩展新 kind
现有分支：`art`→`s.arts`、`tech/dun`→`s.techs`、`array`→`flags`。新增：
```js
} else if (g.kind === 'elixir') {
  s.elixirs[g.ref] = (s.elixirs[g.ref] || 0) + (g.qty || 1);
} else if (g.kind === 'mat') {
  if (!s.materials) s.materials = {};
  s.materials[g.ref] = (s.materials[g.ref] || 0) + (g.qty || 1);
} else if (g.kind === 'equip') {
  grantEquipChecked(s, g.ref);
}
```
> 所有目标 state 字段（`elixirs`/`materials`/`equips` 经由 grantEquipChecked）**均已存在**，无状态层新增风险。

### 2. 灵石价取价规则（维持，不动）
```js
const stoneCost = g.stoneFix || GRADE_STONE[g.grade] || 0;
```
- 功法/遁术/阵法：无 stoneFix → 走 `GRADE_STONE[grade]` 中值。
- 法宝：删除原 `stoneFix`（不再翻倍）→ 灵石价 = `GRADE_STONE[grade]` 中值（**与同档功法灵石一致，不翻倍**）。翻倍仅体现在**功业价**（2× 同档功法基础价）。
- 丹药/灵材：给显式 `stoneFix` 字段即可（灵石标价，不涉及翻倍语义）。

### 3. `sectGoods` 过滤（不动）
已按 `rankMin` 比较 `sectRankIndex`，新商品只需正确填 `rankMin` 即自动按地位解锁。

---

## 四、UI 改动清单（实装时执行）

### 1. 效果文案 `effTxt`（ui.js ~6145）扩展新 kind
当前 `effTxt` 仅处理 tech/dun/array/art。新增分支读取既有表：
- `elixir` → `ELIXIRS[g.ref].desc`
- `mat` → `MATERIALS[g.ref].name + ' ×' + (g.qty||1)`
- `equip` → `EQUIPS[<slot>][g.ref]` 的 `hpMax/atk/wu/ti/dun` 拼接

### 2. 商品卡片（不动结构）
沿用现有 `ct-card` 渲染，仅 `kind` 标签文案扩充（功法/遁术/阵法/丹药/灵材/装备/法宝）。

---

## 五、可选增强（非必需，待定）

| 项 | 说明 | 是否建议 |
|---|---|---|
| 激活 `discount` 字段 | `SECT_RANKS` 已有 1.00/0.95/0.90/0.85/0.80 折扣，引擎未用。可让高阶弟子享灵石折扣 | 建议顺带激活，低成本 |
| 删除 `goods` 死字段 | `SECT_RANKS` 的 `goods:'基础/中级/高级/顶级/全部'` 从未被读取，属误导 | 建议清理 |
| 功业天花板 | 首席需累计 1500 功业，悟道丹+金丹炉即 1600，需另做平衡 | 不在本次范围 |

---

## 六、测试要点（实装后）
1. 外门账号购买聚气丹/黄级灵草 → `s.elixirs.juling`、`s.materials.herb_huang` 正确 +qty。
2. 外门购买青锋剑 → `grantEquipChecked` 入装备栏（s.inventory），攻击 +20 生效。
3. 低地位账号点高阶商品 → 仍报「地位不足」（rankMin 过滤不变）。
4. 法宝灵石 = `GRADE_STONE` 中值（不翻倍）、功业 = 2× 同档基础价（翻倍）→ 断言无误。
5. 复跑自动化套件（当前 76/76），确保无回归。

---

## 七、明确不改动的部分
- **保留通用功法**：宗门继续售卖通用 TECHNIQUES 池（生息/青木/纯阳/天罡/庚金/丙火/麒麟/白虎），不与秘境池做本门专属区分。
- **双货币（灵石+功业）**：维持不变；**翻倍语义锁定为「功业价翻倍、灵石价不翻倍」**（依用户 2026-09-08 澄清）。
- **rankMin 解锁机制**：沿用既有地位过滤，不引入新门槛。
- **stoneFix 字段**：仅用于丹药/灵材/装备的灵石显式定价；法宝已不再使用 stoneFix（灵石回归 GRADE_STONE 中值）。
