# DEDAO 每日登录礼 · 设计 Index

> 状态：**已实装（2026-09-22）** —— 全量测试 340/340 绿，包 `dedao v176` / `dedao_tap v161`
> 定稿日期：2026-09-22
> 定位：账号级（跨存档持久）每日留存功能

---

## 0. 一页速览

| 项目 | 决策 |
|---|---|
| 登录口径 | **本地每日首开**（无后端/无账号，按设备本地自然日判定） |
| 奖励币种 | **纯轮回点**（账号货币 `meta.points`，零依赖存档，绝不会丢） |
| 总量 | **79 点 / 轮**（7 天满勤） |
| 断签规则 | 重置回第 1 天；**Δ=2 时可花 5 轮回点补签**，每周期限 1 次 |
| 呈现方式 | 当天首次开机**自动弹面板** + 标题页**常驻按钮**（未领取带标记） |
| 存档位置 | `meta.daily`（`dedao_meta`，与轮回点/成就同级） |

---

## 1. 数值表（已定稿）

```js
const DAILY_REWARDS = [2, 10, 10, 10, 11, 12, 24];  // index = streak - 1
const PATCH_COST    = 5;                             // 补签单价（轮回点）
```

| 天 | 当日 | 累计 | 环比 | 设计锚点 |
|---:|---:|---:|---:|---|
| 1 | 2 | 2 | — | 尝鲜 |
| **2** | **10** | **12** | **+400%** | **够买【开荒】Lv1→Lv2（6 点），剩 6 点** |
| 3 | 10 | 22 | 持平 | 凑够 Lv2→Lv3（12 点），累计花费 18，开荒池 +12 |
| 4 | 10 | 32 | 持平 | — |
| 5 | 11 | 43 | +10% | — |
| 6 | 12 | 55 | +9% | — |
| 7 | **24** | **79** | **+100%** | 大奖，= 第 6 天的 2 倍 |

**定价锚点依据**（`data.js:3670-3675`）：
- 【开荒】天赋 Lv1→2 = **6 点**、Lv2→3 = **12 点**、Lv3→4 = 18 点
- 轮回阁普通天赋首级单价 **2~5 点**，第 n 级 ×n

**留存逻辑**：第 2 天 10 点 = 第 1 天的 5 倍，制造「回头有惊喜」落差；累计 12 点**正好踩中 6 的倍数**，玩家第 2 天领完即可**当天买下天赋、当天变强**，形成「领→花→爽」闭环——这比单纯多给点更能拉次日留存。

---

## 2. 判定规则

设 `Δ = today - last`（自然日差）：

| 条件 | streak 处理 | 补签 | 说明 |
|---|---|---|---|
| `last` 为空 | `streak = 1` | — | 首次 |
| `Δ = 0` | 不发放 | — | 当日已领，按钮标「已领取」 |
| `Δ = 1` | `streak += 1` | — | 正常连续 |
| `Δ = 1` 且 `streak ≥ 7` | `streak = 1`，`patch = 0` | — | 满勤后开启新一轮 |
| `Δ = 2` | 补签 → `streak += 1`、`patch = 1`；不补 → `streak = 1`、`patch = 0` | ✅ 可用 | 恰好漏 1 天 |
| `Δ ≥ 3` | `streak = 1`、`patch = 0` | ❌ 不可用 | 漏 2 天以上 |
| `Δ < 0` | **不发放、不写 last** | ❌ | 时间倒流，判为异常 |

**补签前置条件**（三条全满足才可补）：
1. `Δ = 2`
2. 本周期 `patch = 0`（每 7 天周期限 1 次）
3. `meta.points ≥ 5`（扣点不能为负）

> **为什么限 Δ=2**：若断了 5 天也能一次补回，等于「5 点买第 7 天 24 点」的套利口子。限制成「只救急一天」，补签就是救济而非刷分。且正常连续登录本就能拿 24 点且不花钱，玩家没有「故意断一天再补」的动机。

---

## 3. 数据模型

```js
// engine.js —— defaultMeta() 增字段
meta.daily = {
  last:   '2026-09-22',  // 上次领取的自然日 YYYY-MM-DD
  streak: 0..7,          // 本周期已连续领取天数；7 = 大奖已领
  total:  12,            // 累计领取次数（预留成就扩展）
  patch:  0 | 1          // 本周期是否已用过补签
}
```

- `defaultMeta()` 补 `daily` 初始值为 `{ last:'', streak:0, total:0, patch:0 }`
- `loadMeta()` 兼容旧档：**无 `daily` 字段视为首次**（不可因缺字段崩溃）
- 日期用 **本地自然日字符串** `YYYY-MM-DD` 比对，绕开时区/夏令时坑

---

## 4. 常量清单（集中可调）

| 常量 | 值 | 位置 | 用途 |
|---|---|---|---|
| `DAILY_REWARDS` | `[2,10,10,10,11,12,24]` | `data.js` 或 `engine.js` 顶部 | 7 天奖励曲线 |
| `PATCH_COST` | `5` | 同上 | 补签单价 |
| `DAILY_CYCLE` | `7` | 同上 | 周期天数 |

> 数值与门槛全部集中在此，调曲线只改一个数组、调门槛只改一个常量。

---

## 5. API 设计（`Engine` 新增）

```js
// 只读查询：不写档、不发奖，供 UI 渲染用
Engine.dailyStatus()
// → { claimed, streak, next, reward, delta, canPatch, patchCost, anomaly }
//   claimed   今日是否已领
//   streak    当前连续天数
//   next      若领取将落到的天数（1..7）
//   reward    若领取可得点数
//   delta     与上次间隔天数
//   canPatch  是否满足补签三条件
//   anomaly   时间倒流等异常

// 执行领取：唯一写档入口
Engine.dailyClaim(usePatch)
// → { ok, msg, reward, streak, patched }
```

内部工具：`todayStr()`（本地自然日）、`diffDays(a, b)`（日差）。

---

## 6. UI 规格

### 6.1 标题页入口
- 新增按钮 `#t-daily`，置于 `t-rebirth`（轮回塔）之后、`t-settings` 之前
- 文案随状态变化：`每日登录礼 · 可领取` / `每日登录礼 · 已领取`

### 6.2 领取面板（7 格）
- 顶部：`连续登录 N 天` + 一句副标题
- 主体：7 格横排（窄屏可 4+3 换行），每格显示「第 N 天」+ 点数
  - **已领**：置灰 + ✓ 标记
  - **当前可领**：高亮描边（主色）
  - **未到**：灰显
- 底部按钮区：
  - 正常：`领取（+N 轮回点）`
  - 断签可补：额外一行提示 + `补签（5 轮回点）` 与 `重新开始`（重置为第 1 天）
  - 已领 / 异常：仅 `关闭`

### 6.3 触发时机
- `boot()` 中、`showScreen('title')` 之后、`Tutorial.autoTitle()` 之前检查
- 当日未领取 → 自动弹面板；已领取 → 静默

---

## 7. 文案清单

| 位置 | 文案 |
|---|---|
| 标题页按钮（可领） | `每日登录礼 · 可领取` |
| 标题页按钮（已领） | `每日登录礼 · 已领取` |
| 面板标题 | `每日登录礼` |
| 面板副标题 | `连续登录 7 天，轮回点逐日递增` |
| 领取按钮 | `领取（+N 轮回点）` |
| 领取成功 | `领取成功，轮回点 +N（已连续 N 天）` |
| 补签按钮 | `补签（5 轮回点）` |
| 补签成功 | `补签成功，轮回点 -5，连续天数已续上` |
| 补签不可用（点数不足） | `轮回点不足 5，无法补签` |
| 补签不可用（已用过） | `本周期补签已用完` |
| 断签提示 | `已断签，不补签则连续天数重置为第 1 天` |
| 时间异常 | `系统时间异常，暂无法领取` |

---

## 8. 涉及文件

| 文件 | 改动 |
|---|---|
| `js/engine.js` | `defaultMeta()` 补 `daily`；`loadMeta()` 兼容旧档；新增 `dailyStatus()` / `dailyClaim()` / `todayStr()` / `diffDays()` |
| `js/data.js` | 新增 `DAILY_REWARDS` / `PATCH_COST`（或置于 engine 顶部，实施时定） |
| `js/ui.js` | `boot()` 挂自动检查；面板渲染；补签交互；标题按钮状态 |
| `index.html` | 标题页新增 `<button id="t-daily">` |
| `css/style.css` | 7 格面板样式（已领/当前/未到三态）+ 按钮标记 |
| `test/automated/18-daily-login.test.js` | **新建**，13 条 |
| `test/automated/run.js` | `MODULES` 追加第 18 行（否则报 `Cannot read properties of undefined (reading 'run')`） |
| `AGENTS.md` | 测试模块表补 18 行 + 用例数 |
| `DEDAO_项目简介.md` | **两处**用例数同步：`325 项用例` → `338 项用例`、`325/325 通过` → `338/338 通过` |

---

## 9. 测试用例清单（13 条）

1. 首次打开 → `streak=1`，`points += 2`，`last = 今天`
2. 同日重复调用 → **不重复发放**（`points` 不变）
3. 隔日 `Δ=1` → `streak=2`，`points += 10`
4. 连续领满 7 天 → 累计 **79** 点
5. 第 7 天后再隔日 → `streak` 归 1，`patch` 归 0，新一轮开始
6. `Δ=2` 不补签 → `streak` 重置 1，只发 2 点
7. `Δ=2` 补签（点数充足）→ `streak` 续 +1，扣 5 点，`patch=1`
8. 补签点数不足（<5）→ 补签不可用，**不扣点**
9. `Δ≥3` → 不可补签，直接重置
10. 每周期补签仅 1 次（第二次 `canPatch=false`）
11. 时间倒流（`last` 在未来）→ 不发放、不推进、不写 `last`
12. meta 往返：`saveMeta`/`loadMeta` 后 `daily` 保持不变
13. 旧档兼容：无 `daily` 字段 → 视作首次，不崩溃

---

## 10. 风险与边界

| 风险 | 处置 |
|---|---|
| **用例总数 325 → 338** | **4 处必须同步**：① `run.js` MODULES ② `AGENTS.md` 模块表（`01-static-data.test.js:525` **逐行比对**数字与 `S.case(` 实际条数，漏改必红）③ `DEDAO_项目简介.md` 的「N 项用例」④ 同文档的「N/N 通过」——后两条是 `:598-599` 的两条断言，挂在**同一个 `total`** 上，只改一处仍会红 |
| 新测试文件写法 | 必须 `return S;` 结尾；钉死随机用 `G.sandbox.Math.random`（改 Node 侧无效）；注释里**不许出现** `S.case(` 字面量，否则被自己数进去导致计数漂移 |
| 时间倒流锁死进度 | `Δ < 0` 判异常：只提示，不发放、不写 `last` |
| 补签扣点变负 | 扣前校验 `points ≥ 5` |
| 改系统时间刷奖励 | 本地方案已知局限（单机、自坑自），不做强防；仅做倒流保护 |
| 旧档缺 `daily` | `loadMeta()` 兜底为首次，禁止直接读属性崩溃 |

---

## 11. 实施步骤

> **已完成（2026-09-22）**：1–9 全部落地。实际用例数 **340**（18 模块 13 条 + 03 UI 端到端 2 条；原 325 → 338 → 340），
> 4 处计数同步点全部对账通过。新增实现要点：`dailyStatus()` 的断签标记字段名为 **`broke`**（非 `broken`——
> `js/` 内有静态守卫禁止出现 `.broken`，那是 2026-09-15 已删除的「突破次数」字段，重名会直接报红）。
> 自动弹面板与新手引导互斥：先弹登录礼，关闭后再调 `Tutorial.autoTitle()`，避免聚光灯与弹窗两层浮层打架。


1. `engine.js`：数据模型 + 判定 + API（先做，纯逻辑）
2. `data.js`：常量
3. 新建 `18-daily-login.test.js`（13 条）+ 同步 `run.js`、`AGENTS.md`
4. 跑全量测试 → 必须全绿
5. `index.html` + `css/style.css` + `ui.js`：UI 与交互
6. 补 UI 相关断言（标题按钮存在、文案随状态变化）
7. 重跑全量测试 → 全绿
8. 两个 TapTap 包各 +1 出包（`dedao` / `dedao_tap`）
9. `git commit` + `git push`（**推送后必须校验 `origin/main` 同步**）
